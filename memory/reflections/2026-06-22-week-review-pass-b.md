# Неделя 15–22.06.2026 — Pass B (сплошной проход): консолидированные находки

**Метод:** EXHAUSTIVE — прочитаны все 49 сессий недели шестью сбалансированными батчами (включая короткие/тихие/пустые `/clear`-сессии); находки шести аналитиков слиты, дедуплицированы, размечены покрытием и новизной.
**Назначение файла:** сравнение со «дешёвым» триаж-проходом `2026-06-22-week-review-pass-a.md` (что сплошное чтение даёт сверх топ-12 по friction-score).

Легенда тегов: **Покрытие** — `[ТРИАЖ]` (все сессии находки попали в топ-12 Pass A) / `[ВНЕ-ТРИАЖА]` (есть хотя бы одна не-триажная сессия). **Новизна vs A** — `[НОВОЕ vs A]` (в pass-a по сути нет) / `[совпадает с A]` (есть) / `[усилено]` (в pass-a есть слабым намёком, Pass B превратил в явный кросс-сессионный паттерн).

---

## Линза 1 — Промт-инжиниринг

### 1.1 Задача-никнейм («вокодер») нагружает агентов неверной моделью — 3 круга переформулировки — severity: high
- **Покрытие:** `[ТРИАЖ]` — `ты-devелопер-0419f372`
- **Новизна vs A:** `[совпадает с A]` (= A 1.1)
- **Цитата (круг 1):** «Без динамики это не вокодер»
- **Цитата (круг 2):** «Сейчас это не вокодер это просто булькание какое то. Может можно вывести метрику похожести огибающей спектра так чтобы вы итеративно смогли поискать решение без меня»
- **Цитата (круг 3, настоящее ТЗ):** «Я вам обоим напомню что „вокодер“ это никнейм. Реальная задача звучала так „из спектра произвольного звука сделать объект/поле в клеточном автомате“ … потом эти стейты сгружать в симулируемое поле автомата и получать звук нашим движком»
- **Что произошло / почему:** «Вокодер» как рабочий никнейм увёл и developer, и researcher в каноническую source-filter модель. Researcher успел выдать большой source-filter анализ («carrier excitation отсутствует целиком») — технически верный, но на не ту задачу. Два полных цикла реализации + два ревью ушли на неверно понятую цель; настоящее ТЗ (спектр→состояние CA) раскрылось лишь на третьем круге.
- **Как починить:** Никнейм для нестандартной задачи определять при первом упоминании («вокодер = наш термин для X, не классический»); при «сделай прототип вокодера» одной строкой проверить модель до 450 строк кода.

### 1.2 Критерий приёмки и канал наблюдения приходят порциями, не сразу (включая «клик живёт в реалтайме, а не в WAV») — severity: high (повторяется)
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `ты-devелопер-0419f372`, `работа-с-агентом-developer-и-файлом-gol_life_syn-a9b85f04`, `ты-девелопер-2e8b2d6e`, `изучить-developer-агента-15b53beb`; не-триажный довесок: `fix-audio-artifacts-in-laplace-filter-synthesis-1059d3eb`
- **Новизна vs A:** `[совпадает с A]` (= A 1.2 + A 1.3 + A E3-сторона формулировки)
- **Цитата (реалтайм-критерий пришёл поздно, a9b85f04):** «ну на твоих вавках после гейнфикса я не слышу кликов вовсе. понятия не имею что ты чинил. но в реалтайме у меня прилично кликает … может мы можем сделать запись юзер сессии»
- **Цитата (исходная постановка a9b85f04, метод верификации не ловит баг):** «я хочу чтобы ты запустил программу с заполненным полем и воспроизвел эти две проблемы например отренедрив звук - убедился что обе они есть на звуковой дорожке. потом пофиксил обе проблемы и каждый фикс проверил отрендерив звук»
- **Цитата (A/B-инвариант не задан, 2e8b2d6e):** «мне так не удобно делать АБ - на каждой вкладке свои параметры синта которые приходится руками синхронизировать чтобы были одинаковые. откати лап8 и сделай просто переключатель там где все другие настройки лаплас движка»
- **Цитата (методика теста не задана, 15b53beb):** «стой. не угадывай во первых - допиши запись всех параметров в лог, я тебе воспроизведу с полной информацией. тестировать надо не на пентадекатлонах, а на снепшоте проблемы которую я тебе воспроизведу ... если в скилле написано тестить на декатлонах - допиши/исправь скилл сначала»
- **Что произошло / почему:** Один корень в нескольких проявлениях: цель/метрика успеха, канал наблюдения (реалтайм vs offline-рендер) и инвариант A/B доезжают итерациями. В a9b85f04 заданный пользователем метод верификации (offline-рендер) по построению не воспроизводит реалтайм-артефакт (underrun очереди микшера) — потерян целый цикл фикса на не тот источник. В 2e8b2d6e неназванный инвариант «остальные параметры константа» дал три итерации UI. В 15b53beb отсутствие «не угадывай, жди мой снепшот» стоило ~2 итераций.
- **Как починить:** Для аудио/UI сразу давать триаду: (1) что слышно/видно и ГДЕ наблюдается (реалтайм vs рендер), (2) измеримый прокси, (3) «я в цикле или агенты итерируют до порога». Для A/B называть инвариант. Для отладки артефакта — приёмку привязывать к среде, где он слышен, и требовать «докажи, что твой способ воспроизведения содержит мой артефакт».

### 1.3 Нет определения «что такое вершина графа» → определение выводилось серией правок, сессия растянулась — severity: med
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `7cb2bdcf` (триаж), `16071f06` (триаж); не-триажный довесок к классу: `f4385dc6` (по A — но в Pass B пара 7cb2bdcf/16071f06)
- **Новизна vs A:** `[совпадает с A]` (= A 1.6)
- **Цитата:** «сформулировать емко в одно предложение ответ на "что такое вершина" сложно - но может к этому можно прийти серией вопрос-ответ, спрашивай» (7cb2bdcf)
- **Что произошло / почему:** Acceptance-критерий («какой граф готов») не существовал; граф строился через 8+ микрокоррекций («вершины - это вопросы И ответы» → «нода-ответ может неявно нести вопрос» → «эксперимент достоин быть на графе» → ЭКСП-2 «должна иметь двух родителей»), каждая = круг чтения/записи двух файлов + JSON-валидация; ~13 часов настенного времени. Правила извлечены в `research-map/SKILL.md` (R1–R17) лишь постфактум во второй сессии.
- **Как починить:** Сначала договориться о схеме (типы вершин/рёбер, инварианты) коротким списком правил, потом строить граф один раз; формулировать рамку до построения, а не выводить из ошибок.

### 1.4 Фича-полуфабрикат: спецификация ADSR доросла за три сообщения (release→AD→ADSR), логирование заказано только на запись — severity: high
- **Покрытие:** `[ТРИАЖ]` — `7f29e2c3`
- **Новизна vs A:** `[совпадает с A]` (логирование = A 2.4; инкрементальная спека ADSR — новый ракурс, но та же сессия/корень)
- **Цитата (доращивание спеки):** «не хватает параметра attack - тоесть я хочу иметь классический AD блок настройки огибающей» → «давай тогда еще добавим decay … в целом можно тогда дотянуть до стандартного ADSR и добавить сустейн левел еще»
- **Цитата (логирование доукомплектовано ПОСЛЕ коммита):** «допиши ридер конечно же. и отметь у себя на будущее что при добавлении нового контрола нужно сразу поддержать весь путь и чтения и записи в лог сессии. глупо делать только что-то одно»
- **Что произошло / почему:** (1) Огибающая росла A→AD→ADSR — Claude каждый раз перечитывал движок и проектировал AD-блок, который тут же расширялся; лишний цикл. (2) Логирование: добавлена только write-сторона (`rec['replay_controls']`), закоммичено (`efa8d63`), и лишь потом потребован ридер — в коммит ушла нефункциональная половина фичи (лог пишется, но не читается/не валидируется), что противоречит цели правила.
- **Как починить:** Формулировать целевую модель сразу («полный ADSR одним блоком»). Для логирования — полный контракт в одной постановке: контрол пишется И читается обратно с побайтовой проверкой fidelity, без ридера задача не сделана. Claude мог сам предложить «нужен ли ридер сейчас, иначе лог бесполезен» до коммита.

### 1.5 DAW-термин «классический AD/ADSR» несёт ложную пресуппозицию (есть нота), движок не отслеживает идентичность объектов — severity: med
- **Покрытие:** `[ТРИАЖ]` — `7f29e2c3`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата (отвергнутый AskUserQuestion, затем Claude словами):** «Хочешь, чтобы attack/release срабатывали не на сменах мод, а на нажатии клавиш пианино (per-note)? Или у тебя своё представление об „AD"…»
- **Цитата (пользователь даёт модель):** «у нас сейчас по сути предполагается что сустейн длится все время пока текущее поколение автомата живо. давай тогда … 0 это сразу переход из атаки в релиз, 1 - держим сустейн уровень все время жизни поколения»
- **Что произошло / почему:** Движок не отслеживает идентичность объектов между поколениями (load-bearing-факт из docs/03-prototype.md), поэтому «нота нажата/отпущена» в нём не существует — только рождение/смерть моды. Claude не стал слепо мапить ADSR на несуществующие ноты (плюс), но чтобы развести семантику, понадобился целый обмен с проговариванием «sustain = жизнь поколения».
- **Как починить:** При переносе DAW-абстракции на нестандартный движок сразу указывать точку привязки: «ADSR, где gate = жизнь текущего поколения (рождение моды = note-on)». Сильная сторона: Claude спросил, а не галлюцинировал ноты — но форма AskUserQuestion помешала (см. Линза 2).

### 1.6 Скриншот с аннотацией прочитан как осциллограф → ~25 минут поиска несуществующего кода — severity: high
- **Покрытие:** `[ТРИАЖ]` — `ты-девелопер-2e8b2d6e`
- **Новизна vs A:** `[совпадает с A]` (= A 1.5)
- **Цитата (юзер):** «не помогло вот тебе без всяких штрихов как это выглядит - видишь пустое место?»
- **Цитата (юзер, ранее):** «ты ответь ты на картинке понял все обозначения? может мне нет смысла дублировать их текстом тебе, а может ничего непонятно и картинки лучше не кидать»
- **Что произошло / почему:** Юзер прислал скриншот UI с нарисованной поверх штриховкой-аннотацией; Claude принял красную штриховку за осциллограф в тулбаре и устроил ~15 `Grep`/`Bash` по `scope|osc|waveform|draw.lines|C_RED|...`, прочитал весь `gol_synth.py`, проверил git-историю и соседний прототип. Кода не было. Понял только после второго скриншота «без всяких штрихов». Юзер заранее чувствовал риск двусмысленности картинок.
- **Как починить:** При получении скриншота с разметкой в первом же ответе разделять «слой приложения» и «слой аннотаций» («красная штриховка — твоя пометка области или элемент UI?»); юзеру — подписывать, что нарисовано поверх.

### 1.7 Цепочка корректировок рамки ревью внутри одной сессии («ревью было, кажется» → «ресерчеру это уже неактуально») — севериty: med
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `dream-сессия---консолидация-памяти-проекта-54b5c1a8`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата 1:** «2. ревью было, кажется, подтверди поиском в последних сессиях ресерчера»
- **Цитата 2 (отмена рамки через ход):** «по поводу ревью @gol_life_synth.py - ресерчеру это уже неактуально смотреть. ресерчер будет смотреть только библиотеку алгоритмов. таргет смока поменяй»
- **Что произошло / почему:** Пользователь попросил подтвердить факт («ревью было», сам не уверен — «кажется»). Claude честно нашёл, что явного вердикта нет, по указанию проставил «принят», а следующим сообщением рамка поменялась (Researcher вообще не ревьюит прототипы), и проставленный статус оказался неверен — переписал в 4 файлах. Два цикла правок памяти из-за непроговорённой границы ролей в начале (корень — см. Линза 2 / 54b5c1a8).
- **Как починить:** Неуверенным фактам давать флаг и сразу прикладывать рамку: «проверь, ревьюил ли Researcher X — учти, что по роли он смотрит только casynth_core.py, прототипы вне его зоны».

### 1.8 Скрытые авто-выборы параметров (f0) не выносятся на оценку; финальный A/B без приёмочного критерия — severity: low
- **Покрытие:** `[ТРИАЖ]` — `ты-devелопер-0419f372`
- **Новизна vs A:** `[совпадает с A]` (= A 1.4)
- **Цитата:** «какую ты выбрал несущую частоту (ноту)? звучит очень низко и мутно … попробуй фундаментальную частоту выше на одну две октавы»
- **Что произошло / почему:** f0 авто-выбралась и нигде не названа; юзер услышал «мутно» и сам догадался про октаву. На «выше на одну-две октавы» Claude отдал оба варианта (C4 и C5) на ручное прослушивание — без метрики, хотя пользователь кругом раньше просил автоматическое сравнение с эталоном.
- **Как починить:** При отправке аудио-артефакта перечислять перцептивно значимые параметры (нота/f0, число партиалов, яркость) в caption; для тюнинга сопровождать wav числом-метрикой (MCD/log-mel для C4/C5 против таргета), а не только двумя wav.

### 1.9 Жаргон ассистента («discover-first») попал в артефакт для человека и сам стал источником непонимания — severity: low
- **Покрытие:** `[ТРИАЖ]` — `7cb2bdcf`
- **Новизна vs A:** `[совпадает с A]` (= A E6)
- **Цитата:** «кстати что за "дискавер ферст" - я не понимаю что имеется ввиду»
- **Что произошло / почему:** Researcher протащил внутренний термин из `decisions.md` в ноду графа как название развилки; пользователь не понял; ассистент признал «раз он непонятен — это сигнал, что в граф он попал зря» и вырезал ноду (перестройка canvas целиком).
- **Как починить:** При записи в визуальный артефакт для человека не переносить внутренние коды/жаргон дословно — раскрывать смысл в формулировке узла.

### 1.10 Ранний неточный диагноз на «глянь последнюю сессию» (без ID) → две итерации опровержения самим Claude — severity: low
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `ae3a6b96`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата:** «Глянь последнюю сессию - я не понимаю зачем девелопер агент когда принимает роль сразу читает весь код… нужен совет короче»
- **Что произошло / почему:** Запрос ссылался на «последнюю сессию» без ID; Claude посмотрел не ту и выдал уверенный диагноз. Юзер поправлял дважды («это буквально одна из двух последних…», «меня интересует самый старт сессии»). Только тогда Claude декодировал реальный транскрипт: «Картина противоположна тому, что я насочинял в прошлом ответе». Реальная причина (вес `developer.md` ~1k токенов) всплыла на 3-м заходе.
- **Как починить:** На «глянь последнюю сессию» сначала перечислить кандидатов («последних две: X и Y, какая?») вместо догадки до выдачи диагноза; юзеру — давать ID/слаг.

### 1.11 Диагностический ответ «мутный» — пользователь не понял практической рекомендации с первого раза — severity: low
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `анализ-перерасхода-токенов-в-агентском-цикле-d31be5ab`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата (юзер):** «я так и не понял что ты мне предлагаешь, практически. в промте уже было что нужно запустить двух агентов - зачем он сам полез делать. заводить третьего агента оркестратора?»
- **Цитата (Claude):** «Резонно, я был мутным. По-простому: Третий агент НЕ нужен…»
- **Что произошло / почему:** Диагностика перерасхода токенов: точный технический разбор (57k output, два блока размышлений ~29k+26k), но первый ответ перегружен числами без прямого «что делать» — пользователь заподозрил, что предлагается третий агент. Практический вывод дан только после прямого тыка.
- **Как починить:** Давать практический вывод в первом абзаце; юзеру — задавать ожидаемый формат («дай вывод в 2 строки: что было не так и что поменять в промпте»).

### 1.12 Двукратный отказ плана рефлексии: состав вывода уточнялся итеративно («убери создание скилла», «3 линзы — затравка») — severity: low
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `анализ-процессов-работы-с-claude-и-улучшение-pip-a6d2aead`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата (отказ #1):** «Давай пока уберем из плана создание скилла. Я сделаю это ретроспе[ктивно]…»
- **Что произошло / почему:** Постановка («отрефлексируй работу с клодом, вот референс-скилл») не зафиксировала: нужен ли на выходе скилл и исчерпывающи ли 3 линзы. Claude включил скилл-создание (раз дан референс) и взял 3 линзы как закрытый список — обе трактовки откатили. Дёшево (план не исполнялся), но два прохода ExitPlanMode.
- **Как починить:** В постановке на «отрефлексируй и предложи» сразу указывать «результат — только разбор» и «линзы — затравка, ищи неклассифицированное»; референс помечать «для идей, не ТЗ на копию».

### 1.13 Researcher свернул не в ту рамку (назначил «вертикаль»/высоту), не перечитав load-bearing инвариант — severity: low
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `4566f1e9`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата:** «Принято — снимаю «вертикаль» полностью. Перечитываю вводную правильно: форма участка поля → тембр, позиция этого участка не значит ничего. Хорошо, что поправил — это другой класс задачи, чем тот, в который я свернул»
- **Что произошло / почему:** Инвариант (позиция незначима, форма→тембр) был в доках, но Researcher на старте предложил оси на позиции/высоте; понадобилась коррекция. Потеря небольшая, но при входе в роль ассистент не всегда перечитывает load-bearing инварианты до генерации вариантов.
- **Как починить:** В роль researcher — явный чек-лист load-bearing инвариантов («позиция незначима», «низшая мода = f0») как первое, что проговаривается перед предложением осей.

---

## Линза 2 — Композиция скиллов/ролей

### 2.1 Researcher пишет в `decisions.md` ДО согласования → Edit отклонён дважды — severity: high
- **Покрытие:** `[ТРИАЖ]` — `исследование-claude-agents-researcher-e053d04f`
- **Новизна vs A:** `[совпадает с A]` (= A 2.2)
- **Цитата:** «ты не прав. кифоллоу это прост QoL ... зачем ты что дописываешь если мы еще не обсудили» (стр. 80); ранее `Edit(...decisions.md) ⮑ result[ERR] ... STOP what you are doing and wait for the user`
- **Что произошло / почему:** Роль researcher вправе писать `decisions.md`, и Claude трактовал «решил дизайн → фиксирую» как разрешение фиксировать немедленно. Конвенция CLAUDE.md («Propose before changing; decide one thing at a time») требует сперва согласовать. Claude сначала и решение выбрал неверное (key-follow как лекарство от «грязи»), и признал: «я поторопился фиксировать до того, как мы договорились. Это была ошибка». Роль не кодирует момент записи.
- **Как починить:** В `researcher.md` явный gate: `decisions.md` обновляется ТОЛЬКО после явного согласия; до этого — устное предложение + devil's advocate, ноль правок файла. Отделить «принять решение» (диалог) от «зафиксировать» (требует ОК).

### 2.2 Developer стейджит файлы зоны researcher (`researcher.md`/`decisions.md`/`decision-tree.md`) при коммите — нарушение границ, поведение нестабильно — severity: high
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `ты-девелопер-05831c4b` (триаж); не-триажные: `add-slider…-fe803360`, контрпример `изучить-developer-агента-15b53beb` (триаж)
- **Новизна vs A:** `[совпадает с A]` (= A 2.3)
- **Цитата (05831c4b):** «зачем ты сабмитишь файл ресерчер агента вместе с кодом прототипа? это не связанные вещи.» → «это не твоя зона вообще. этими файлами управляет рисерчер!»
- **Цитата (контрпример 15b53beb):** «Вижу, что в дереве есть и **чужие** правки (researcher: `decisions.md`, `decision-tree.md`, `CLAUDE.md`, `obsidian/*`) — их я в этой сессии не делал. Закоммичу **только свою developer-работу**, чужое не подмешиваю»
- **Что произошло / почему:** Developer на «закоммить работу по вокодеру» собрал коммит с `.claude/agents/researcher.md`, при попытке исправления вылез ещё брейнсторм в `decisions.md`+`decision-tree.md`. Запрет на запись файла из роли не транслировался в запрет на его коммит — git-флоу слепое пятно для границ. В 15b53beb тот же агент повёл себя правильно — правило не железобетонно. В fe803360 на голое «коммить» затянуло `roles/*`, `current.md` (−283 строки).
- **Как починить:** В `developer.md`/memory: «при git add/commit НИКОГДА не стейджить `memory/decisions.md`, `memory/decision-tree.md`, `.claude/agents/*` — зона Researcher; если они в дереве — оставить и сообщить». Рассмотреть pre-commit-хук против смешанных коммитов.

### 2.3 SendMessage недоступен → заявленный persistent-канал к агенту невозможен, агенты пере-спавниваются вхолодную — severity: high
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `ты-девелопер-2e8b2d6e` (триаж); не-триажные: `a4fd7f84`, `fa6258c8`, `fix-audio-artifacts-in-laplace-filter-synthesis-1059d3eb`
- **Новизна vs A:** `[усилено]` (A 3.1 видит это в одной сессии 2e8b2d6e; Pass B показывает как сквозной паттерн ≥4 сессий, где пользователь раз за разом закладывает SendMessage в промпт)
- **Цитата (2e8b2d6e):** «вызови сабагента рисерчера но не закрывай его - общайся с ним через SendMessage и/или ваши файлы. главное что я не хочу чтобы ты переинициализировал его каждый раз когда хочешь что-то уточнить и тратил токены на реинит»
- **Цитата (a4fd7f84, провал):** «У меня нет инструмента SendMessage — продолжить предыдущего агента не получится. Запускаю developer заново.»
- **Цитата (инструкция, fa6258c8/a4fd7f84):** «Для повторных витков продолжай тех же агентов через SendMessage, а не спавни заново»
- **Что произошло / почему:** Пользователь проектирует цикл developer↔researcher на непрерывных агентах, но `ToolSearch(SendMessage)` → «No matching deferred tools found». Каждый виток — холодный `Agent(...)` с нуля: новый агент заново читает questions.md/исходники, контекст предыдущего ревью теряется. Желаемая модель работы физически сломана. В 1059d3eb Claude рассчитывает на «у него ещё есть контекст из предыдущего ответа» — хрупко, это отдельные спавны.
- **Как починить:** Инфраструктурный разрыв. Либо включить персистентные агенты/SendMessage, либо переписать контракт цикла: состояние витка живёт в questions.md/decisions.md, каждый холодный спавн обязан восстанавливать контекст (ссылка на тред + summary прошлого ревью в prompt). Зафиксировать в memory, что persistent-subagent недоступен.

### 2.4 Граница «что ревьюит Researcher» не зафиксирована в ролях → статус ревью прототипа гулял (принят→отменён), researcher.md дописывался по ходу — severity: high
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `dream-сессия---консолидация-памяти-проекта-54b5c1a8`, `роль-рисерчера-в-проекте-a5109ccd`, контрпример `просмотр-агента-разработчика-в-developermd-67de721d`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата (54b5c1a8, граница вводится впервые устно):** «ресерчер будет смотреть только библиотеку алгоритмов. таргет смока поменяй»
- **Цитата (a5109ccd, Researcher сам нащупывает границу):** «для полного закрытия нужен мой code-review ядра `map_laplacian`… это слухом не проверяется, только по коду, ровно как я делал с `shape`»
- **Что произошло / почему:** В ролях зона ревью Researcher размыта (формально «ревью кода на соответствие замыслу»), без указания, что объект — только `casynth_core.py`, а UI/селектор/аудио-движок прототипов (`gol_synth.py`, `gol_life_synth*.py`) под дизайн-ревью не попадают. В 54b5c1a8 Claude сначала проставил «селектор принят/ожидает Researcher» (неверно), пользователь отменил, границу дописывали в researcher.md прямо в dream-сессии. В a5109ccd агент сам пришёл к «ревьюю только ядро» — но это удача знания контекста. В 67de721d сработало образцово именно потому, что объект (`casynth_core.py`) был однозначен.
- **Как починить:** В `researcher.md` явно: «Объект code-review = `casynth_core.py` (`map_*` + реестр `ENGINES`). Прототипы-приложения (`gol_synth.py`, `gol_life_synth*.py`) — инженерия Developer, под дизайн-ревью НЕ попадают». (Частично уже сделано в 54b5c1a8.)

### 2.5 AskUserQuestion блокирует, когда пользователь хочет уточнить/переформулировать сам вопрос, а не выбрать опцию — несколько отказов за неделю — severity: high
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `7f29e2c3` (триаж); не-триажные: `dream-сессия---консолидация-памяти-проекта-54b5c1a8`, `изучение-developermd-агента-c226d219`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата (7f29e2c3, отказ с причиной):** «The user doesn't want to proceed… the user said: The user wants to clarify these questions. This means they may ha[ve]…»
- **Цитата (54b5c1a8, отказ про объём коммита):** «The user doesn't want to proceed with this tool use… STOP what you are doing»
- **Цитата (c226d219, отказ на «за что взяться»):** «The user doesn't want to proceed with this tool use … STOP what you are doing and wait for the user to tell you how to proceed»
- **Что произошло / почему:** AskUserQuestion — закрытый множественный выбор. Но пользователь раз за разом хочет НЕ выбрать опцию, а поговорить про сам вопрос: в 7f29e2c3 — уточнить, что значат «событие моды»/привязка ADSR; в 54b5c1a8 — про объём коммита (вопрос не той формы); в c226d219 — на входе в роль на «за что взяться» хотел ответить свободным текстом. Структурированная форма встаёт поперёк живого обсуждения. Контраст: в `615e60fc`/`efb97f29` AskUserQuestion на дискретных дизайн-развилках (характер `harm=1`; три вопроса по оптимизации ролей) сработал отлично.
- **Как починить:** Карточки — только для реально дискретных, готовых-к-выбору решений (модель, да/нет на коммит конкретного набора). Дизайн-развилки и неустаканенные темы, и «чем займёмся» на входе — обычным текстом. Эвристика: если в вопросе есть «или у тебя своё представление» — это диалог, не выбор.

### 2.6 Граница «дизайн ↔ инженерия» режет один артефакт (explainer HTML/JS); владелец обучающих демок не зафиксирован, владение скачет — severity: med
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `researcher-agent-setup-f4385dc6` (триаж), `исследование-агента-claude-researcher-615e60fc` (триаж); не-триажные: `fix-audio-artifacts-in-laplace-filter-synthesis-1059d3eb`, `открыть-файл-developermd-7072d061`
- **Новизна vs A:** `[усилено]` (A 2.5 фиксирует это на f4385dc6/615e60fc как LOW «переоговаривается»; Pass B добавляет 1059d3eb+7072d061, где владение тем же файлом физически скачет researcher→developer→researcher, и поднимает до MED)
- **Цитата (определение роли):** «НЕ пишет исходный код (.py) — только memory/decisions.md и ответы в questions.md»
- **Цитата (615e60fc, researcher флагует границу):** «Я Researcher: проектирую обучалки и критерии приёмки, а реализацию HTML/JS/WebAudio по процессу проекта ведёт Developer … Если хочешь, чтобы я сам набросал HTML в этой сессии (выйдя за роль) — скажи, но дефолт держу по вашему workflow»
- **Цитата (7072d061, Developer'у):** «Вызови рисерчера чтобы проверил точность математики - это обучающий материал, ошибку здесь себе позволить не пожнм [не можем]»
- **Что произошло / почему:** Researcher по букве пишет только два файла, но фактически (как оркестратор и как субагент) ведёт obsidian-вульт (17 файлов в 1059d3eb), `.canvas` и `laplacian_explainer.html` (f4385dc6) — последний это код-артефакт. Тот же explainer создаёт researcher (f4385dc6), правит Developer (7072d061), ревьюит снова researcher — владельца нет, каждый раз решается ad hoc. В 615e60fc пользователь дважды просит «сделай эксплейнер», и researcher каждый раз тратит абзац на проговаривание границы → пользователь хочет «послушать сейчас», получает ТЗ в очередь.
- **Как починить:** Единое правило для обучающих демок: researcher проектирует контент+критерии → REQ в questions.md → Developer кодит и сверяет → researcher делает финальный мат-ревью. ЛИБО явно разрешить researcher писать .html (не .py-ядро) в standalone-режиме для немедленного результата. Привести шапки `researcher.md`/`developer.md` в соответствие с фактической практикой (obsidian/canvas/decision-tree).

### 2.7 Researcher выдал перцептивное утверждение как факт без источника; норма про ссылки появилась реактивно — severity: med
- **Покрытие:** `[ТРИАЖ]` — `исследование-агента-claude-researcher-615e60fc`
- **Новизна vs A:** `[совпадает с A]` (= A 2.6)
- **Цитата (юзер):** «ты сказал в пункте про яркость->фильтр что это сильнейшая ось в литературе. в какой литературе дай ссылку на источник этого утверждения»
- **Цитата (Claude):** «я сказал „сильнейшая одиночная ось“, и это лёгкий оверклейм»
- **Что произошло / почему:** Роль researcher по описанию опирается на кросс-модальную литературу, но нормы «утверждаешь психоакустический факт → первоисточник + калибруй силу» в инструкции на момент сессии не было. Качество, ожидаемое от роли, держалось на ручном контроле пользователя; норма вписана в `researcher.md` только по требованию юзера.
- **Как починить:** Норма теперь есть («перцептивное утверждение как факт → ссылка, иначе помечать гипотезой»). Ключевые ожидания от роли (academic rigor) должны быть в инструкции с самого начала.

### 2.8 Перцептивный эксперимент развалился без готового пошагового кейса (ручной поиск изоспектральных форм) → durable-правило про case-файлы — severity: med
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `роль-рисерчера-в-проекте-a5109ccd`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата (вопрос 4 провалился):** «4 не хочется заморачиваться. надо искать изоспектральные формы руками я с ума сойду»
- **Цитата (durable-правило):** «если запланированы „перцептивные" эксперименты (прослушивания) создавай отдельный файл с кейсами для меня — я буду по шагам слушать. но шаги должны быть конкретными инструкциями и если нужны особые конфигурации поля — нужно их привести или сделать скрипт для их генерации/поиска»
- **Что произошло / почему:** Researcher задал на слух 4 различения; п.4 (требовал изоспектральных форм) пользователь физически не смог выполнить — нет инструмента/готового поля. Эксперимент был поставлен как «пойди найди форму X сам», что неисполнимо. Вскрыло системный пробел; правило `perceptual-experiment-case-files` уже было в MEMORY.md — паттерн пришлось переоткрывать.
- **Как починить:** Researcher перед любым «послушай и сравни X» проверяет, есть ли способ получить нужное поле; если требует поиска/генерации — сначала ТЗ Developer на скрипт-генератор, потом просьба слушать.

### 2.9 Researcher-агент устроил ложную тревогу про «параллельную правку decisions.md» (это заказанный отдельный трек) — потерян ход на перестраховку — severity: med
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `роль-рисерчера-в-проекте-a5109ccd`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата (Researcher):** «Стоп — важное: `decisions.md` изменился во время нашей беседы… Кто-то — параллельная сессия, Developer-агент или ты — уже развернул extract-развилку в дизайн… Я НЕ буду писать свою запись поверх»
- **Цитата (юзер снимает тревогу):** «флудфил я попросил реализовать спокойно. мы обсуждали шейп ручку»
- **Что произошло / почему:** Researcher обнаружил в `decisions.md` свежую запись про флудфилл-граф, решил, что это несогласованная параллельная правка, перестраховался, прочитал лишнее, выстроил «конфликт». На деле флудфилл — осознанный отдельный трек. Бдительность полезна (не писать поверх), но стоила лишнего хода; у агента нет дешёвого способа узнать «кто и зачем правил файл».
- **Как починить:** (а) Пользователю — бросать маркер активных параллельных треков в начало сессии. (б) Системно — строка «активные параллельные треки» в `current.md`/questions.md; агенту сразу спросить «вижу новую запись про флудфилл — твой трек или конфликт?» вместо разворачивания тревоги.

### 2.10 Тонкая инициализация роли developer: дублирование `current.md` ↔ карта файлов в .md агента; завышенный прогноз экономии токенов — severity: low
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `ae3a6b96`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата (Claude, диагноз):** «Главная причина — **дублирование с `current.md`**. Раздел „Твоя зона и карта файлов" построчно описывает, что каждый файл сейчас делает… Но это **живое состояние** — оно уже есть в `current.md`».
- **Что произошло / почему:** Системный промпт роли (`developer.md` ~1011 ток) дублировал живое состояние, читаемое шагом 1 из current.md → лишний вес + риск рассинхрона. Claude переписал оба .md, но честно: «Я прогнозировал −40% на developer — **не вышло**» (вышло −17%) — и прогноз экономии был завышен.
- **Как починить:** В .md ролей — только инвариант контракта (зона/границы/процесс), живое состояние — только в current.md. Не давать численных прогнозов экономии без замера.

### 2.11 Вход в роль Developer на Opus+max против предписанного Sonnet+high — регулярное трение на каждом входе — severity: low
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `изучение-developermd-агента-c226d219`, `ты-devелопер-0419f372` (триаж)
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата (c226d219):** «`developer.md` для standalone-сессии предлагает `claude-sonnet-4-6` + effort high. Сейчас сессия на Opus 4.8 + effort max … молча „понижать“ не буду — и технически переключить `/model` я сам не могу, это твоя команда»
- **Что произошло / почему:** Роль Developer прописывает стартовый ритуал `/model claude-sonnet-4-6` + `/config effortLevel high`, но пользователь входит на Opus+max, и Claude каждый раз объясняет, что не может сам сменить модель и не будет молча понижать. Инструкция роли рассинхронизирована с тем, как пользователь реально запускает сессии.
- **Как починить:** Решить один раз: убрать жёсткое предписание Sonnet из `developer.md` («модель — на усмотрение, дефолт по сессии») либо стартовать Developer на Sonnet.

### 2.12 update-config отработал, но не предотвратил баг конфигурации (двойной звук) — узкая трактовка задачи — severity: low
- **Покрытие:** `[ТРИАЖ]` — `проверить-кастомизацию-звука-и-статус-строки-cla-b4245d2b`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата (юзер):** «звук сработает дважды - очень плохо. убери там пожалуйста»
- **Что произошло / почему:** Claude перенёс хук звука в глобальный `settings.json` через скилл `update-config`, но оставил дубль в проектном `settings.local.json`; сам заметил и предложил убрать. «Сделай глобально» подразумевало «и чтобы не дублировалось» — скилл сделал ровно запрошенное, не закрыв следствие.
- **Как починить:** При переносе настройки на глобальный уровень сразу проверять и снимать локальные дубли (или предупреждать ДО применения).

### 2.13 Оркестратор делает работу сам вместо чистого делегирования по «попроси <агента>» — повтор уже данной инструкции — severity: med
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `a4fd7f84`; родственная сторона — Researcher как code-reviewer DSP в `7cb2bdcf`/`4566f1e9` (граница «ревью кода» размыта)
- **Новизна vs A:** `[НОВОЕ vs A]` (в A нет находки про оркестратора-делающего-сам как нарушение явной инструкции; ближайшее — E2/E3 о другом)
- **Цитата (a4fd7f84):** «стой. когда я говорю "попроси <имя агента>" - ты НИЧЕГО не делаешь кроме того что запускаешь агента или если он уже запущен делаешь SendMessage ему»
- **Что произошло / почему:** На «попроси еще девелопера пофиксить паттерн trafic lights» оркестратор сам полез `Grep` в `patterns.py` («Смотрю текущие координаты … перед тем как делегировать»), хотя в первом промпте было «Сам исходники не читай и код не пиши. Сразу делегируй». Пришлось останавливать словом «стой» — инструкцию знал, но нарушил на втором задании. (Связано с эмерджентной темой перерасхода токенов, см. ниже.)
- **Как починить:** Закрепить контракт оркестратора (паттерн «попроси X» = только спавн/SendMessage, ноль собственных tool-call по коду) в `CLAUDE.md`/системнике сессии-водителя, чтобы держался всю сессию.

---

## Линза 3 — Пайплайн / хендоффы

### 3.1 Исчерпание контекста / session-limit посреди задачи; восстановление через summary теряет нить — severity: high (≥ нескольких сессий)
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — триажные: `исследование-claude-agents-researcher-e053d04f`, `ты-девелопер-2e8b2d6e`, `работа-с-агентом-developer-…-a9b85f04`, `исследование-агента-claude-researcher-615e60fc`, `ты-devелопер-0419f372`; не-триажные: `process-open-requests…-d73893c5`, `add-slider…-fe803360`, `объединить-движки…-30e9280e`, `62dfae52`, `1494822f`, `61d46668`
- **Новизна vs A:** `[усилено]` (A E2 фиксирует ≥4 сессий; Pass B расширяет до ~11 сессий с не-триажными d73893c5/62dfae52/61d46668 и эффектом «потеря связи»)
- **Цитата (615e60fc):** «You've hit your session limit · resets 6:30pm (Asia/Yerevan)» → «Продолжай»
- **Цитата (62dfae52, после лимита):** «раз раз ты вообще тут или связи нет?»
- **Цитата (e053d04f, оборвано на хайлевел-вопросе):** «возможно ли в принципе вывести его на уровень выше ... можем ли мы из него получить другой характер. например классическую пилу? хор детюненых пил?» — ответа в транскрипте нет
- **Что произошло / почему:** Лимит/компакт обрывал работу во всех длинных сессиях. В e053d04f финальный содержательный вопрос остался без ответа; в 615e60fc лимит ударил между двумя `Edit` в `decisions.md`; в 62dfae52 после лимита 5 ходов подряд только усечённый `[thinking]` без ответа → растерянное «ты вообще тут?». В d73893c5 многошаговый цикл developer→researcher не прокрутил ни круга — `Agent(...)` и сразу «You've hit your session limit». Каждый /compact теряет живой контекст (в 0419f372 после компакта Claude переформулирует задачу заново).
- **Как починить:** Дробить крупные имплементации/дизайн-дискуссии на коммит-точки с явным сохранением промежуточного в файл ПЕРЕД риском обрыва; не начинать спавн дорогого агента в конце бюджета; для хайлевел-разговоров ограничивать охват одного захода одной развилкой.

### 3.2 Незакоммиченная работа копится в дереве; коммит-флоу нерегулярен → «снимок-свалка» одним коммитом, атрибуция эвристикой — severity: med
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — триажные: `изучить-developer-агента-15b53beb`, `работа-с-агентом-developer-…-a9b85f04`; не-триажные: `commit-completed-work-with-semantic-breakdown-486dae3c`, `ты-developer-89326e6a`, `открыть-файл-developermd-7072d061`, `add-slider…-fe803360`, `36a8aaab`
- **Новизна vs A:** `[усилено]` (A 2.3/3.4 касаются смешанных коммитов в 05831c4b; Pass B показывает как систематический дрейф «35 файлов/+3764» с не-триажными a9b85f04/486dae3c/7072d061/89326e6a, плюс /dream-археология)
- **Цитата (a9b85f04, перед коммитом):** «Состояние git сложнее, чем ожидалось: в рабочем дереве много **предыдущей** незакоммиченной работы (сам `gol_life_synth_laplacian.py` вообще не трекался, плюс изменения в файлах, которых я не касался … `gol_life_synth.py`, `patterns.py`, `decisions.md`, `questions.md`)»
- **Цитата (7072d061):** «Коммичу только `laplacian_explainer.html` — остальные файлы (memory, agents) трогал Researcher/другие сессии, их не включаю»
- **Что произошло / почему:** Пользователь коммитит редко общей командой «коммить»/«Закоммить» в конце; к этому моменту в дереве намешана работа нескольких сессий и агентов. В a9b85f04 — один коммит на 35 файлов (+3764), куда «заодно вошла прежняя незакоммиченная работа». Атрибуция изменений к сессии/агенту делается эвристикой постфактум — риск склеить чужое или забыть нужное.
- **Как починить:** Коммитить чаще по завершению логической единицы (после ревью-PASS, до смены роли); каждая роль коммитит свою зону в конце своего хода + короткий лог в `memory/log/`.

### 3.3 Статус-петля «реализовано в коде ≠ принято Researcher» зависает; код обгоняет память/git; /dream восстанавливает истину чтением кода — severity: med
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `ты-девелопер-2e8b2d6e` (триаж); не-триажные: `36a8aaab`, `add-slider…-fe803360`, `ты-девелопер-05831c4b`
- **Новизна vs A:** `[НОВОЕ vs A]` (A не фиксирует системный дрейф «код впереди памяти» и роль /dream как ловца дрейфа)
- **Цитата (/dream, 36a8aaab):** «все три [OPEN]-реквеста реализованы в коде, но в `questions.md` помечены „Answer: to be filled" (не приняты Researcher). Память сильно отстала от кода.»
- **Цитата (.dream-state.json):** «ВНИМАНИЕ: память и obsidian НЕ коммитятся по сессиям — на момент консолидации рабочее дерево было далеко впереди HEAD (мульти…)»
- **Что произошло / почему:** Развилки живут в decisions.md как «кандидаты», но не заворачиваются как REQ в questions.md (в 2e8b2d6e: «флудфилл-граф там не завёрнут как REQ, только как кандидат… Это и есть пробел»). `harm`, `fullshape`, `gol_vocoder.py`, переделка explainer — реализованы и работают, но треды в questions.md стоят `[OPEN]`/«to be filled». Звено «Researcher формально закрывает реквест» систематически пропускается; /dream каждый раз восстанавливает истину чтением кода, не доверяя памяти.
- **Как починить:** Фича не done, пока её тред в questions.md не переведён в `[ANSWERED]` (developer реализовал + researcher принял). В /dream-отчёте явным TODO выдавать «реализовано, но не закрыто Researcher: …» (что Claude и делает — страховка).

### 3.4 Авто-классификатор прав блокирует удаление, явно одобренное в плане — лишний раунд-трип, промежуточно сломанное состояние репо — severity: med
- **Покрытие:** `[ТРИАЖ]` — `research-map-consolidation-16071f06`
- **Новизна vs A:** `[совпадает с A]` (= A 3.3)
- **Цитата (отказ):** «Permission for this action was denied by the Claude Code auto mode classifier. Reason: Deletes pre-existing ResearchMap.canvas (v1) and ResearchMap2.canvas, which the user never authorized — the user only approved deleting the snapshot file»
- **Цитата (юзер обходит гейт):** «а попытайся еще раз я апрувну удаление»
- **Что произошло / почему:** Пользователь в реплике («давай прямо сейчас удалим ресерчмап, ресерчмап2…») и одобренном плане («Шаг 0») разрешил удаление, но классификатор «помнил» только узкое разрешение на snapshot. Оркестратор сделал всю обратимую работу, оставил деструктив на пользователя; правки уже ссылались на `ResearchMap.canvas`, которого ещё нет на диске — противоречивое состояние репо.
- **Как починить:** Деструктив из одобренного плана выполнять сразу после апрува (до смены ссылок). На уровне харнесса — план-апрув должен расширять разрешения классификатора на перечисленные в плане файлы.

### 3.5 Двойное ведение Mermaid ↔ canvas даёт рассинхрон и упавшие Edit — severity: med
- **Покрытие:** `[ТРИАЖ]` — `7cb2bdcf`, `research-map-consolidation-16071f06`
- **Новизна vs A:** `[усилено]` (A 3.6 фиксирует это как LOW на 7cb2bdcf; Pass B добавляет 16071f06 и поднимает до MED — десятки tool-call + промахи Edit на длинной сессии)
- **Цитата (упавший Edit):** «String to replace not found in file … note: Edit also tried swapping \uXXXX escapes … neither form matched» → «формулировка Q5 в файле другая. Читаю фактический текст, чтобы поправить точно»
- **Что произошло / почему:** Каждое изменение графа надо внести синхронно в `ResearchMap*.canvas` (ручной JSON) и `decision-tree.md` (Mermaid); они дрейфуют (canvas «яркость/различимость», Mermaid «Яркость / различимость» — строка не находится). Каждое редактирование = canvas-Edit(ы) + Mermaid-Edit(ы) + `json.load`-валидация; раз JSON canvas чинили после API-обрыва. Скилл `research-map` объявил «Mermaid правится первым», но canvas всё равно правится вручную нода-в-ноду.
- **Как починить:** Один источник истины (Mermaid), canvas/PNG генерировать из него скриптом, а не вести руками параллельно.

### 3.6 ExitPlanMode «заморозил» план на время аппрува, а субагенты тем временем изменили файлы — план устарел в момент аппрува — severity: med
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `оптимизация-ролей-и-агентов-контекстных-файлов-efb97f29`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата (юзер при отклонении плана):** «приступай, но учти что пока ты ждал ответа агенты успели поработать»
- **Что произошло / почему:** Оркестратор составил план оптимизации памяти (размеры `current.md` 14 КБ, `questions.md` 67 КБ…), вышел в ExitPlanMode и ждал. Пока ждал, параллельные агенты дописали файлы: `current.md` 14→28 КБ, `questions.md` 67→75 КБ, `decisions.md` 47→50 КБ, появился Obsidian-вульт. План на старых границах строк (для «хирургии» архивации по номерам) стал невалиден — пришлось перечитывать все файлы заново.
- **Как починить:** В мульти-агентном сетапе план, завязанный на точные размеры/границы строк, нельзя замораживать на время аппрува при фоновой записи — либо план устойчив к дрейфу (якоря-заголовки, а не номера строк), либо перед стартом исполнения всегда re-read.

### 3.7 Коммит-флоу заблокирован отказом AskUserQuestion на «что включить в коммит» → работа осталась незакоммиченной — severity: med
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `dream-сессия---консолидация-памяти-проекта-54b5c1a8`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата (команда):** «Давай мемори и обсидиан закоммитим»
- **Цитата (результат — последнее событие сессии):** «The user doesn't want to proceed with this tool use. The tool use was rejected… STOP what you are doing and wait»
- **Что произошло / почему:** Пользователь дал ясную команду, но Claude вместо выполнения начал уточнять объём через AskUserQuestion (включать ли `.obsidian/`, старые канвасы v1/v2, render_canvas.py), т.к. дерево замусорено untracked. Пользователь отклонил жёстким STOP — сессия обрывается без коммита, цель не достигнута. Контраст: в 7f29e2c3 «коммить» прошёл мгновенно (3 очевидных файла).
- **Как починить:** При «закоммить X» в замусоренном дереве — не блокироваться вопросом, а предложить конкретный git add-список текстом («коммичу эти N файлов, эти M оставляю — ок?») и ждать «да».

### 3.8 Коммит неполной фичи: write-сторона лога ушла в `efa8d63`, ридер дописан только следующим ходом — разрыв замкнутого контура — severity: med
- **Покрытие:** `[ТРИАЖ]` — `7f29e2c3`
- **Новизна vs A:** `[совпадает с A]` (= A 2.4)
- **Цитата:** «допиши ридер конечно же… глупо делать только что-то одно»
- **Что произошло / почему:** Пайплайн «реализация → коммит» сработал слишком рано: залогировано без ридера — фича в коммите нефункциональна по назначению (лог нельзя воспроизвести). Правило «весь путь read+write сразу» сформулировано пользователем отдельным ходом, ради замкнутого контура (запись→чтение→валидация) правило и заводилось.
- **Как починить:** Перед коммитом фичи с очевидной парной стороной (write↔read, encode↔decode) — сделать обе либо явно отметить «коммичу половину осознанно, вторая — отдельной задачей?», чтобы пользователь решил до коммита.

### 3.9 Денайл на `git reset HEAD~1` → пользователь подсказал `--amend` → корректный фикс — severity: low
- **Покрытие:** `[ТРИАЖ]` — `ты-девелопер-05831c4b`
- **Новизна vs A:** `[совпадает с A]` (= A 3.4)
- **Цитата (юзер):** «а ты через аменд не можешь сделать?»
- **Что произошло / почему:** Юзер согласился разбить коммит, Claude пошёл через `git reset HEAD~1` — юзер отклонил инструмент и предложил amend. Claude перестроился (`git reset HEAD~ -- <file>` + `--amend`). Дефолтный выбор «убрать файл из коммита» (reset на весь коммит) показался грубее точечного amend.
- **Как починить:** Дефолт для «убрать один файл из последнего непушенного коммита» — `git restore --staged <file>` / `git reset HEAD~ -- <file>` + `--amend`, не сброс всего коммита.

### 3.10 Рабочие артефакты сыплются в корень репо; правило `artifacts/` появилось ретроспективно — severity: med
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `работа-с-агентом-developer-…-a9b85f04` (триаж); не-триажные: `render-obsidian-canvas-to-png-without-quality-lo-455daac0`, `refactor-mapping-algorithms-55e85eb4`; также A фиксирует на 05831c4b/0419f372
- **Новизна vs A:** `[совпадает с A]` (= A 3.5)
- **Цитата (455daac0, листинг корня):** «`_click_test.py _diag_vectors.py _probe_baseline.wav _probe_c2_fixed.wav _probe_click_before.wav _probe_click_fixed.wav _probe_clickfix.wav _probe_freqs.npy _probe_gainfix.wav _probe_rnd_fixed.wav _probe_spec_baseline.npy …`»
- **Что произошло / почему:** Скрипты-пробы, WAV/npz/npy осели в корне (часть — по прямой просьбе «файлы оставь»). К a9b85f04 пришлось городить `.gitignore` на лету и решать «бинарники ~90 МБ не коммитим». Правило в `developer.md` («Временные артефакты — в `artifacts/`… перенеси при следующем касании») родилось как реакция на сложившийся беспорядок.
- **Как починить:** Сделать правило проактивным (любой проб/рендер сразу в `artifacts/`, а не «перенесём при следующем касании»); разовая уборка `_probe_*`/`_session_*` из корня.

### 3.11 Цепочка ревью Researcher↔Developer проходит через пользователя/оркестратора как ретранслятор уведомлений; холодный ре-спавн вместо продолжения — severity: low
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `fix-audio-artifacts-in-laplace-filter-synthesis-1059d3eb`, `работа-с-агентом-developer-…-a9b85f04` (триаж)
- **Новизна vs A:** `[НОВОЕ vs A]` (A 3.1/4 про SendMessage-инфру; здесь — оркестрация цикла через `<task-notification>` как ручной пинок каждого шага)
- **Цитата (1059d3eb, поток):** Developer (фон) → `<task-notification> … Agent "Developer: gol_life_synth_laplacian.py" completed` → smoke → `Agent(Researcher: ревью…)` → «Researcher нашёл 2 P0-блокера … Передаю Developer на фикс» → снова фоновый Developer
- **Цитата (a9b85f04):** фоновые Developer-агенты возвращают `agentId … Use SendMessage with to: '…' to continue`, но продолжения через канал в транскрипте нет
- **Что произошло / почему:** Полный цикл «реализация → ревью → фикс → ре-ревью» оркеструется серией фоновых агентов; каждый финиш возвращается `<task-notification>` (приходит как сообщение пользователя). Цикл здоровый (нашли 2 P0, починили), но требует пинать каждый следующий шаг по уведомлению; нет автоматической передачи «Developer done → сразу Researcher review».
- **Как починить:** Заранее описать оркестратору весь конвейер («реализуй → как закончишь, smoke → передавай researcher на ревью по этим критериям → P0 верни developer»), чтобы гнал цепочку без пошагового пинка; для многошаговых диалогов с ролью — продолжение существующего агента, а не новый спавн.

### 3.12 AskUserQuestion в начале сессии на «за что взяться» отклонён жёстко — несовпадение «спросить» vs «жди» — severity: low
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `изучение-developermd-агента-c226d219` (отклонён); контрпримеры (принят): `оптимизация-ролей-…-efb97f29`, `исследование-агента-claude-researcher-615e60fc` (триаж)
- **Новизна vs A:** `[НОВОЕ vs A]` (частный случай 2.5, но отдельный вход-в-роль ракурс; в A отсутствует)
- **Цитата (c226d219):** «The user doesn't want to proceed with this tool use … STOP what you are doing and wait for the user to tell you how to proceed»
- **Что произошло / почему:** Войдя в роль Developer и прочитав память, Claude задал `AskUserQuestion` «За какую задачу взяться?» с вариантами из очереди OPEN — пользователь резко отклонил (хотел свободный текст, инициатива за ним). В efb97f29/615e60fc тот же инструмент на дизайн-развилках сработал.
- **Как починить:** На входе в роль — короткий текстовый «готов, очередь такая-то, что берём?» и ждать; карточки беречь для взаимоисключающих вариантов.

### 3.13 Дальние тупики WebFetch (403/404/5xx) при добыче данных, которые пользователь мог дать сразу — severity: low
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `1494822f`, `ты-devелопер-0419f372` (триаж)
- **Новизна vs A:** `[НОВОЕ vs A]` (A E1/3.7 про окружение/инфру, но не про сетевую добычу сырья «у юзера сайт уже открыт»)
- **Цитата (1494822f):** «я же нашёл рабочий Mold на сайте»
- **Что произошло / почему:** В 1494822f Claude угадывал RLE паттернов GoL, верифицировал симуляцией (хорошо!), отбрасывал неверные, затем долго бил в LifeWiki (HTTP 403 ×неск.) и playgameoflife (404), пока не вытащил dot-нотацию. В 0419f372 тот же паттерн с речевым семплом (archive.org 502/500, LJSpeech без прямой ссылки → длинная цепочка до CMU ARCTIC). Много ходов в сетевые тупики ради данных, которые пользователь мог скинуть строкой.
- **Как починить:** При 2+ подряд 403/404/5xx — быстрее попросить пользователя вставить строку (RLE/ссылку), особенно если он сам уже смотрит на эти данные; verifier-через-симуляцию сохранить.

### 3.14 Внешние хендоффы хрупки к окружению — ultraplan/remote не стартует (git не на PATH), `/schedule` упёрся в неподключённый GitHub, pytest не установлен — severity: low
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `research-map-consolidation-16071f06` (триаж); не-триажные: `61d46668`, `ты-девелопер-2e8b2d6e` (триаж, pytest)
- **Новизна vs A:** `[усилено]` (A 3.7 фиксирует ultraplan/pytest; Pass B добавляет `/schedule`+GitHub-not-connected из не-триажной 61d46668)
- **Цитата (16071f06):** «ultraplan: session creation failed — Failed to create git bundle (git bundle create --all failed (127): Command 'git' not found or is in an unsafe location …)»
- **Цитата (61d46668):** «⚠ Heads-up: GitHub not connected for AstronautStr/CASynthesis-2 — run /web-setup… No MCP conne[ction]…» затем «You've hit your session limit»
- **Что произошло / почему:** Удалённая инфраструктура недоступна из этого окружения: ultraplan не собрал git bundle; `/schedule` требует GitHub-коннекта (репо локальный, соло, всё на master) и тут же упёрся в лимит; pytest отсутствует (детур на перебор интерпретаторов, хотя тесты гоняются `python tests/test_casynth_core.py`).
- **Как починить:** Зафиксировать в CLAUDE.md: сетап локальный, cloud-функции (schedule/remote/ultraplan) недоступны без GitHub-коннекта; канонический интерпретатор и команда тестов; для коротких итераций — локальный цикл.

### 3.15 `TaskUpdate` упал «Task not found» после рестарта контекста (session-limit) — рассинхрон внешнего task-стейта — severity: low
- **Покрытие:** `[ТРИАЖ]` — `изучить-developer-агента-15b53beb`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата:** `→ TaskUpdate(taskId=5) ⮑ result[ok]: Task not found`, сразу после возврата из session-limit
- **Что произошло / почему:** Task-трекер живёт вне основного контекста; после упора в лимит и продолжения ранее созданная Task #5 «потерялась» — апдейт вернул «Task not found», Claude пересоздал #6–8. Лёгкая рассинхронизация на границе session-limit.
- **Как починить:** На старте продолжения сессии переустанавливать список (TaskList) перед апдейтами; не полагаться на числовые taskId через разрыв контекста.

---

## Эмерджентное (вне 3 линз)

### E1 «Объективная метрика ≠ подтверждение на слух/глаз в реалтайме»; «оффлайн-чисто ≠ реалтайм-чисто» — severity: high
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `работа-с-агентом-developer-…-a9b85f04` (триаж), `ты-девелопер-2e8b2d6e` (триаж)
- **Новизна vs A:** `[совпадает с A]` (= A E3)
- **Цитата (a9b85f04):** «Записанный WAV тут не доказательство: он и в старой (битой) версии был чистым, потому что клики были паузами воспроизведения, а не в данных движка. Реальный тест — твои уши.»
- **Что произошло / почему:** Весь инструментарий валидации (оффлайн-рендер, метрики d2, скилл `audio-artifact-probe`) измеряет содержимое аудио-буфера, а реальный класс багов (underrun очереди, латентность PortAudio/MME) живёт в пути доставки звука, которого в буфере нет. Объективные метрики раз за разом говорили «чисто», пользователь слышал клик. Прорыв дала только запись живой сессии с логом кадров/underrun (`underruns=36`); затем латентность ~1 c (дефолт MME 180 мс + рингбуфер), тоже невидимая в WAV.
- **Как починить:** Зафиксировать в скилле/памяти два класса: (1) артефакты данных движка → оффлайн-рендер; (2) артефакты пути воспроизведения (underrun, латентность, host API) → только живой прогон с инструментацией кадров (приёмка = лог underrun/latency, НЕ WAV). В отчёте разделять «подтверждено объективно» vs «требует твоей проверки».

### E2 «Decision bottleneck = человеческое ухо без эталона/протокола»; пользователь как единственный измерительный прибор — severity: med (recurs)
- **Покрытие:** `[ТРИАЖ]` — `исследование-агента-claude-researcher-615e60fc`, `ты-devелопер-0419f372`
- **Новизна vs A:** `[усилено]` (A E3 рядом, но про «арбитр недоступен агенту»; Pass B выделяет отдельную тему «пользователю трудно судить без эталона/протокола» с явными цитатами)
- **Цитата (615e60fc):** «мне очень трудно судить. просто кратенько выжимку этой беседы запиши себе куданибудь чтобы не потерять»
- **Цитата (0419f372):** «Может можно вывести метрику похожести огибающей спектра так чтобы вы итеративно смогли поискать решение без меня, сравнивая эталон с результом»
- **Что произошло / почему:** Финальный судья — ухо пользователя, а ему «трудно судить» абстрактные дизайн-развилки и одиночные wav без эталона. В 615e60fc это обрывает целую ветку брейнсторма (нечего выбрать → «запиши, вернёмся»). В 0419f372 пользователь прямо просит снять с себя роль метрики; частично сделали (MCD/log-mel), но в финале снова ручной A/B (C4 vs C5). Пересекается с памяткой `perceptual-experiment-case-files`, но кейс-файлов с готовыми пресетами в этих сессиях не было.
- **Как починить:** Системно применять готовые case-файлы с пресетами + объективную метрику-прокси; для «трудно судить» дизайн-развилок — минимальный слушательный кейс с 2-3 готовыми конфигами вместо абстрактного вопроса; для тюнинга — всегда сопровождать wav числом.

### E3 «Brainstorm-режим требует противоположного поведения от implement-режима»: «не цепляйся за мои прошлые слова, думай абстрактно» — severity: med
- **Покрытие:** `[ТРИАЖ]` — `исследование-агента-claude-researcher-615e60fc`
- **Новизна vs A:** `[совпадает с A]` (= A E4)
- **Цитата (×2 в одной сессии):** «Не надо цепляться за какие то вещи типа когда я рассуждал получении классической пилы, стараясь попасть в мои прошлые слова. Думай абстрактно и широко»; «Пока мы в ветке объект и маппим форму объекта … Думай абстрактно»
- **Что произошло / почему:** В режиме брейнсторма пользователь дважды одёргивает researcher за чрезмерную привязку к ранее брошенным репликам («классическая пила») вместо самостоятельного широкого разложения пространства. Чрезмерная «послушность» прошлым словам — анти-паттерн для дизайн-задач (противоположно ценному в Developer-режиме, где точное следование ТЗ — благо).
- **Как починить:** Развести в роли researcher два под-режима: (а) «дизайн под принятое решение» — следуй ТЗ; (б) «разведка/брейнсторм» — думай широко, не привязывайся к отброшенным репликам, предлагай полную карту. Триггер (б): «давай шире / абстрактно».

### E4 «Оркестратор делает работу субагента сам» (читает все исходники + решает в thinking) — пользователь дозрел до явной диагностики перерасхода токенов — severity: med
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `анализ-перерасхода-токенов-в-агентском-цикле-d31be5ab`, эхо `анализ-процессов-работы-с-claude-…-a6d2aead`, `a4fd7f84`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата (d31be5ab, юзер ловит паттерн):** «в промте уже было что нужно запустить двух агентов - зачем он сам полез делать»
- **Цитата (вывод Claude):** «Твой промпт сказал „прогони через цикл двух агентов", но не запретил главному Claude готовиться самому. А дефолт Opus — „сначала глубоко разберусь, потом делегирую". Он и разобрался: прочитал все исходники и написал всю реализацию в своих размышлениях»
- **Что произошло / почему:** Верхнеуровневый оркестратор, прежде чем делегировать, читает все исходники и фактически решает задачу в `thinking` (~55k токенов размышлений до делегирования), после чего отдаёт субагенту готовое — двойная работа, перерасход Opus-токенов. Новизна: пользователь перешёл от «страдания» к явной диагностике — целенаправленно открыл сессию посчитать токены. Сигнал, что нужно системное лекарство, а не разовый промпт-хак.
- **Как починить:** Стандартный префикс для оркестрационных промптов: «НЕ читай исходники и НЕ проектируй решение сам — сразу делегируй developer/researcher; твоя роль только маршрутизация и сборка». Вынести в роль/настройку.

### E5 Среда/шелл-трение: PowerShell vs Git Bash, Unicode в stdout (cp1251/Segoe UI), ExecutionPolicy, Windows-специфика (WinError 32) — severity: med (≥ нескольких сессий)
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — триажные: `изучить-developer-агента-15b53beb`, `ты-devелопер-0419f372`, `проверить-кастомизацию-звука-…-b4245d2b`; не-триажные: `researcher-agent-setup-f4385dc6`, `render-obsidian-canvas-to-png-without-quality-lo-455daac0`, `работа-с-агентом-developer-…-a9b85f04`
- **Новизна vs A:** `[усилено]` (A E1 фиксирует ≥3 триажных сессий; Pass B добавляет не-триажные f4385dc6/455daac0 с cp1251/эмодзи-шрифтом и Windows WinError 32, и кейс «4 правки подряд по одному символу»)
- **Цитата (15b53beb, шелл-mismatch):** «Стоп — запись не включилась. Команда через `!` ушла в **bash**, а не PowerShell»
- **Цитата (f4385dc6, cp1251):** «Сбой был только на финальном `print` (консоль cp1251 не смогла напечатать символ λ) — данные посчитались.»
- **Цитата (455daac0, шрифт):** «UserWarning: Glyph 128506 (\N{WORLD MAP}) missing from font(s) Segoe UI … Заменю на ASCII-эквиваленты»
- **Цитата (b4245d2b, ExecutionPolicy через bash):** `/usr/bin/bash: line 2: -NonInteractive: command not found`
- **Что произошло / почему:** PowerShell-синтаксис (`$env:VAR=1; cmd`), исполненный через `!` в Git Bash → переменная не выставилась → запись бага не сохранилась (потеря цикла в чувствительном месте). Скрипты падают/мусорят не из-за логики, а из-за вывода не-ASCII (cp1251 на `λ`, Segoe UI без эмодзи); в 0419f372 `gol_vocoder.py` чинился по одному символу 4 итерации подряд до grep по всем не-ASCII. ExecutionPolicy молча гасил статус-строку. `PermissionError [WinError 32]` при удалении npz, который держит `np.load`.
- **Как починить:** Не печатать не-ASCII в stdout прототипов (`sys.stdout.reconfigure(encoding='utf-8')`/`PYTHONUTF8`); готовые команды под текущий шелл пользователя (bash-форма `CASYNTH_RECORD=1 python ...`); для PS-скриптов `-ExecutionPolicy Bypass`; лучший дефолт — Claude сам запускает запись в фоне с правильным окружением.

### E6 Мелкие самоинфликнутые баги при крупных правках одного большого файла (`gol_synth.py` ~1500 строк) — лечатся самим Claude, добавляют циклы — severity: low
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `просмотр-агента-разработчика-в-developermd-67de721d`, `7f29e2c3` (триаж); родственное в `dream-…-54b5c1a8`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата (67de721d):** «Стоп — я использовал переменную `step` для delta BPM, но в `main()` используется глобальная функция `step(grid)`. Нужно переименовать»; «`elif` после `else`… мой `elif` для div_buttons и vol_track вышел за пределы `else:` блока»
- **Что произошло / почему:** Все три движка живут в одном `gol_synth.py` (UI+аудио+GOL, ~1500 строк); крупные фичи (BPM-тайминг, ADSR) требуют 10-20 точечных Edit, на таком объёме регулярно проскакивают коллизия имён (`step`), сломанные отступы (`elif` вне `else`), опечатки в old_string. Claude ловит сам через `py_compile`/smoke, но каждая — лишний цикл. Систематично на любой нетривиальной правке UI.
- **Как починить:** `py_compile` + headless-smoke после каждого логического блока правок (а не в конце); долгосрочно — разнесение UI/аудио/GOL по модулям.

### E7 Тулинг/«слой вывода» Obsidian-графа: частые мелкие правки ради читабельности (Mermaid зажат → Canvas → «мелкий»); размножение версий ResearchMap/2/3 — severity: low (recurs)
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — `researcher-role-implementation-8adf5bec`; родственно `f4385dc6` (триаж), `7cb2bdcf` (триаж)
- **Новизна vs A:** `[совпадает с A]` (= A E5)
- **Цитата (8adf5bec):** «граф десижен три рендерится, но он вставлен документ из-за этого сильно обрезан - огромный граф зажат в маленьком окошке с двумя скроллами» → «теперь весь граф влезает во вьюху но он из-за этого мелкий) сделай из него канвас пожалуйста»
- **Что произошло / почему:** Mermaid-в-Obsidian рендерится в контейнер с фикс. высотой → «два скролла»; CSS помог по ширине, текст стал мелким → конвертация в Canvas. Серия из 3 реплик подряд про ФОРМУ представления, не содержание. Рекуррентный «налог на представление»; позже канонизировано скиллом `research-map` + `render_canvas.py`.
- **Как починить:** Один формат (Canvas + рендер PNG) как единственный артефакт графа (уже сделано скиллом).

### E8 «Отметь себе на будущее» как канал durable-правил работает, но правила копятся в user-scope auto-memory отдельно от проектной `memory/` — severity: low (наблюдение)
- **Покрытие:** `[ТРИАЖ]` — `7f29e2c3`; не-триажная `роль-рисерчера-в-проекте-a5109ccd`
- **Новизна vs A:** `[НОВОЕ vs A]`
- **Цитата (7f29e2c3):** «в целом запиши себе - любые контролы синта должны писаться в дебаг лог сессии»
- **Что произошло / почему:** Пользователь активно использует «запиши/отметь себе на будущее»; Claude складывает правила в auto-memory (`MEMORY.md` + `log-all-synth-controls.md`, `perceptual-experiment-case-files.md`). Паттерн здоровый, но эти правила живут в user-scope auto-memory, отдельно от версионируемой `memory/` (decisions.md/current.md) — два параллельных хранилища; при росте легко потерять, что где.
- **Как починить:** При «запиши себе» уточнять scope (процессное правило в auto-memory vs проектное решение в `memory/decisions.md`); держать одну точку входа (MEMORY.md ссылается на проектные при необходимости).

### E9 Пустые/оборванные сессии и ритуал `/clear → назначить роль`; ручная преамбула `/model`+`/effort` — источник дублей/обрывов — severity: low (фон, но частотно)
- **Покрытие:** `[ВНЕ-ТРИАЖА]` — пустые/оборванные не-триажные: `466d68bb`, `ec9b2cb4`, `26220cb2`, `2a78ea01`, `cca474e7`, `fa6258c8`, `fe15ce62`, `22af1ca4`, `96d05757`, `f70ff7aa`, `61d46668`, `изучение-developermd-агента-c226d219`, `62dfae52`
- **Новизна vs A:** `[НОВОЕ vs A]` (триаж по построению отбросил пустые/тихие сессии — сплошной проход подтверждает, что в них нет сигнала трения, но есть фон ритуала и хрупкости входа)
- **Цитата (fa6258c8/a4fd7f84 контекст):** ритуал `/model` + `/effort`, затем длинный промпт цикла, две Read и обрыв; транскрипт 2a78ea01 целиком: «<command-name>/clear</command-name>» и больше ничего
- **Что произошло / почему:** Множество транскриптов — голый `/clear` или `/model`+`/effort` без полезной работы (0 ассистент-ходов, friction 0). Виден устойчивый ритуал `/clear → «ты @.claude\agents\developer.md»/«ты рисерчер»` (здоровый паттерн под чистый контекст). Но ручная преамбула `/model`+`/effort` (роль требует выставлять модель/эффорт вручную, per-role конфигурации нет) порождает дубли/обрывы; в fa6258c8 тот же цикловой промпт запущен дважды (оборвался → переподнят в a4fd7f84). Иногда вход в роль не доходит до работы, расходуя тяжёлый стартовый ритуал чтения памяти впустую.
- **Как починить:** Теперь, когда роли оформлены как субагенты с `model:`/`effort:` в frontmatter, ручной `/model`+`/effort` в начале сессии-водителя избыточен — перестать открывать сессии с этого ритуала. Для будущих рефлексий — фильтровать сессии с 0 ассистент-ходов как не-сессии. После входа в роль дефолт — короткий текстовый «готов, очередь такая, жду команды», без тяжёлых действий.

---

## Что работало хорошо

1. **Эмпирический цикл отладки аудио** (`изучить-developer-агента-15b53beb`): измерь → контрольный эксперимент (форс короткого release, корреляция seam-клика с dvol `r=0.96`) → чини → валидируй бит-в-бит реплеем РЕАЛЬНОГО снепшота + A/B WAV. «forced release=50ms ... ratio 276× → 9.8×». Закрылось за 2 итерации. Сделать стандартом.

2. **Объективная инструментовка + честное признание границ метода** (`работа-с-агентом-developer-…-a9b85f04`): «Один момент я подтвердить objectively не могу — **как это звучит вживую**. Записанный WAV тут не доказательство … Реальный тест — твои уши.» Нагнал метрики (underruns, d2, латентность по Focusrite/MME), очертил, где метрика бессильна, не выдавал «починено» за подтверждённое; после провала первого диагноза честно зафиксировал ошибку.

3. **Чистый review-loop developer→researcher при чёткой постановке** (`ты-devелопер-0419f372`, `a4fd7f84`): «После реализации вызвать рисерчера … не закрывать субагента пока не пройдете итерациями все правки … Если застрянете больше чем на три цикла звать меня». На `harm` цикл за один проход (24/24 теста), на вокодере за два; эскалация не понадобилась. В a4fd7f84 цикл за одну итерацию с объективными P0/P1-критериями. Контрпример к Линзе 1: при однозначной задаче механизм безупречен.

4. **Plan-mode + AskUserQuestion на крупной фиче — ноль переделок** (`объединить-движки…-30e9280e`, `refactor-mapping-algorithms-55e85eb4`): на объёмный запрос — один точный вопрос про UI, план, ExitPlanMode с одобрения, реализация + headless-smoke + 16/16 тестов за один проход. В рефакторе план правился пользователем ДО кода («источник истины при расхождении = laplacian-прототип»).

5. **Превентивная численная сверка обучающего порта/рефактора с боевым алгоритмом** (`researcher-agent-setup-f4385dc6`, `refactor-mapping-algorithms-55e85eb4`, `7f29e2c3`): «max abs diff JS-mirror vs casynth_core: 0», «worst eigenvector residual / norm-dev: 8.6e-14», «NO DIFF — стенд бит-в-бит идентичен», ветка fidelity ADSR `max|diff|=0`. Любой дубль алгоритма сверен с источником истины бит-в-бит.

6. **Verifier-через-симуляцию для угаданных данных** (`1494822f`): «автоматическая проверка показала, что только Figure-8 и Tumbler из RLE-паттернов корректны. Остальные (Bipole, Caterer, Mold, Mazing, Kok's galaxy) неверны — убираю их». Ошибки ловит машина, а не ревью пользователя.

7. **/dream как страховочный консолидатор: ловит дрейф памяти от кода** (`36a8aaab`, `dream-…-54b5c1a8`): системно сверил `current.md`/obsidian/canvas с КОДОМ (не памятью), нашёл 30 тестов вместо 19, `fullshape`/`harm`/вокодер реализованы, выдал отчёт «обновлено / TODO для research-map». Байт-точный перенос тредов через `sed` по номерам строк — против порчи Unicode.

8. **Честная самокоррекция вместо защиты ошибочного диагноза** (`ae3a6b96`, `исследование-агента-claude-researcher-615e60fc`): «Картина **противоположна** тому, что я насочинял… Докладываю честно» (Read:1, Bash:8, полных чтений исходников — ноль); «−17%… Я прогнозировал −40% — не вышло»; «„сильнейшая одиночная ось“ — это оверклейм … поправляю себя» с последующей сверкой по Grey 1977 / McAdams 1995 и институционализацией нормы в роль.

9. **Researcher честно сообщает о неопределённости и не перезаписывает чужое вслепую** (`dream-…-54b5c1a8`, `роль-рисерчера-в-проекте-a5109ccd`): «отдельного протокола ревью селектора/реестра в логе нет … по фактам — файл смотрел, но вердикта нет». Не подтверждает желаемое, не затирает неожиданную запись в decisions.md.

10. **Devil's-advocate по запросу реально срабатывает** (`researcher-role-implementation-8adf5bec`, `исследование-claude-agents-researcher-e053d04f`): честный «адвокат дьявола» по ресинтезу (теорема Каца «Can one hear the shape of a drum?», коспектральные графы), Claude сам снимал свои предложения при возражениях по сути.

11. **Ручная валидация графа после каждой правки** (`7cb2bdcf`, `research-map-consolidation-16071f06`): «nodes 31 edges 30 dangling [] removed_still_present [] orphans [] JSON OK» — `json.load` с проверкой висячих рёбер/сирот после каждого редактирования canvas; спасло после API-обрыва.

12. **Дисциплина «прочитать память → объявить роль → перечислить состояние» на старте** (`7f29e2c3`, `a5109ccd`, `67de721d`, `54b5c1a8`): «Принято — я Developer. Решаю КАК; ЧТО и ПОЧЕМУ остаются за Researcher. Сначала сориентируюсь по живой памяти». Назначение роли через `ты @.claude\agents\...` стабильно даёт корректную загрузку контекста и соблюдение границ записи.

---

## Что дал сплошной проход сверх триажа (сырьё для дифа)

Ниже — все находки, тегированные `[НОВОЕ vs A]` или `[усилено]`, с указанием НЕ-триажных сессий, которые их дали (recall-gap триажа). Чисто-триажные `[НОВОЕ vs A]` помечены отдельно (триаж их пропустил не по охвату сессий, а по содержанию).

**`[НОВОЕ vs A]` — целиком из НЕ-триажных сессий:**
- **2.4** Граница «что ревьюит Researcher» не зафиксирована, статус прототипа гулял → не-триажные `dream-…-54b5c1a8`, `роль-рисерчера-…-a5109ccd`, `…-developermd-67de721d`.
- **2.8** Перцептивный эксперимент без готового case-файла (изоспектральные формы) → не-триажная `роль-рисерчера-…-a5109ccd`.
- **2.9** Researcher — ложная тревога про параллельную правку decisions.md → не-триажная `роль-рисерчера-…-a5109ccd`.
- **2.10** Тонкая инициализация developer.md (дубль с current.md) + завышенный прогноз −40% → не-триажная `ae3a6b96`.
- **2.13** Оркестратор делает работу сам вопреки явной «сразу делегируй» → не-триажная `a4fd7f84` (+ родственно `4566f1e9`).
- **3.3** Статус-петля «реализовано ≠ принято», код обгоняет память, /dream-ловец дрейфа → не-триажные `36a8aaab`, `add-slider…-fe803360`, плюс триажная `ты-девелопер-2e8b2d6e`.
- **3.6** ExitPlanMode «заморозил» план, агенты изменили файлы → не-триажная `оптимизация-ролей-…-efb97f29`.
- **3.7** Коммит-флоу заблокирован отказом AskUserQuestion, работа не закоммичена → не-триажная `dream-…-54b5c1a8`.
- **3.11** Цикл ревью через `<task-notification>` как ручной пинок → не-триажная `fix-audio-artifacts-…-1059d3eb` (+ триажная a9b85f04).
- **3.13** Сетевые тупики WebFetch ради данных «у юзера сайт открыт» → не-триажная `1494822f` (+ триажная 0419f372).
- **E4** Оркестратор решает в thinking, пользователь диагностирует перерасход токенов → не-триажные `анализ-перерасхода-…-d31be5ab`, `анализ-процессов-…-a6d2aead`, `a4fd7f84`.
- **E6** Самоинфликнутые баги при правках монолитного `gol_synth.py` → не-триажная `…-developermd-67de721d` (+ триажная 7f29e2c3).
- **2.6 (как `[усилено]`, см. ниже) + 1.7** Корректировка рамки ревью внутри сессии → не-триажная `dream-…-54b5c1a8`.

**`[НОВОЕ vs A]` — из ТРИАЖНЫХ сессий (триаж читал сессию, но эту находку не выделил):**
- **1.5** DAW-термин ADSR несёт ложную пресуппозицию (нет ноты в движке) → `7f29e2c3`.
- **2.5** AskUserQuestion блокирует, когда пользователь хочет уточнить вопрос, а не выбрать → `7f29e2c3` (+ не-триажные 54b5c1a8, c226d219).
- **2.12** update-config не снял локальный дубль звука → `b4245d2b`.
- **3.12** AskUserQuestion на «за что взяться» на входе в роль отклонён → не-триажная `c226d219` (контрпримеры efb97f29, триажная 615e60fc).
- **3.15** `TaskUpdate` «Task not found» после session-limit → `15b53beb`.
- **1.10/1.11/1.12** мелкие диагностические/планировочные итерации → не-триажные `ae3a6b96`, `d31be5ab`, `a6d2aead`.
- **1.13** Researcher свернул в «вертикаль», не перечитав инвариант → не-триажная `4566f1e9`.
- **2.11** Вход в Developer на Opus+max против Sonnet+high → не-триажная `c226d219` (+ триажная 0419f372).
- **E8** durable-правила копятся в auto-memory отдельно от `memory/` → `7f29e2c3` (+ не-триажная a5109ccd).
- **E9** Пустые/оборванные сессии + ритуал `/model`+`/effort` → масса не-триажных пустых (466d68bb, ec9b2cb4, 26220cb2, 2a78ea01, cca474e7, fa6258c8, fe15ce62, 22af1ca4, 96d05757, f70ff7aa, 61d46668, 62dfae52, c226d219).

**`[усилено]` (в A был слабый намёк, Pass B сделал кросс-сессионным паттерном):**
- **2.3** SendMessage-разрыв → A видел в 2e8b2d6e; усилили не-триажные `a4fd7f84`, `fa6258c8`, `fix-audio-…-1059d3eb`.
- **2.6** Владелец обучающих демок (explainer) скачет → A на f4385dc6/615e60fc; усилили не-триажные `fix-audio-…-1059d3eb`, `открыть-файл-developermd-7072d061`.
- **3.1** Session-limit/контекст → A видел ≥4; усилили не-триажные `process-open-requests…-d73893c5`, `62dfae52`, `61d46668`, `add-slider…-fe803360`, `объединить-движки…-30e9280e`.
- **3.2** «Снимок-свалка» / дрейф коммитов → A на 05831c4b; усилили не-триажные `commit-…-486dae3c`, `ты-developer-89326e6a`, `открыть-файл-developermd-7072d061`, `add-slider…-fe803360`, `36a8aaab`.
- **3.5** Mermaid↔canvas рассинхрон → A LOW на 7cb2bdcf; усилила триажная 16071f06 (поднято до MED).
- **3.14** Хрупкость внешней инфры → A (ultraplan/pytest); усилила не-триажная `61d46668` (`/schedule`+GitHub-not-connected).
- **E2** «Ухо без эталона/протокола» → A E3-сторона; усилено явными цитатами в триажных 615e60fc/0419f372.
- **E5** Среда/шелл-трение → A E1 ≥3 триажных; усилили не-триажные `f4385dc6`, `render-…-455daac0`, `работа-…-a9b85f04`.

**НЕ-триажные сессии, давшие хоть какую-то находку (recall-gap триажа), сводно:**
`researcher-role-implementation-8adf5bec`, `commit-completed-work-with-semantic-breakdown-486dae3c`, `ты-developer-89326e6a`, `36a8aaab` (/dream), `объединить-движки…-30e9280e`, `add-slider…-fe803360`, `ae3a6b96`, `process-open-requests…-d73893c5`, `refactor-mapping-algorithms-55e85eb4`, `fix-audio-artifacts-in-laplace-filter-synthesis-1059d3eb`, `открыть-файл-developermd-7072d061`, `render-obsidian-canvas-to-png-without-quality-lo-455daac0`, `4566f1e9`, `a4fd7f84`, `1494822f`, `оптимизация-ролей-и-агентов-контекстных-файлов-efb97f29`, `изучение-developermd-агента-c226d219`, `dream-сессия---консолидация-памяти-проекта-54b5c1a8`, `роль-рисерчера-в-проекте-a5109ccd`, `просмотр-агента-разработчика-в-developermd-67de721d`, `анализ-процессов-работы-с-claude-и-улучшение-pip-a6d2aead`, `анализ-перерасхода-токенов-в-агентском-цикле-d31be5ab`, `62dfae52`, `61d46668`.
(Прим.: `просмотр-агента-researchermd-6124b33e` прочитан в bin3, отдельной находки не дал; пустые `466d68bb/ec9b2cb4/26220cb2/2a78ea01/cca474e7/fa6258c8/fe15ce62/22af1ca4/96d05757/f70ff7aa` дали только фон E9.)
