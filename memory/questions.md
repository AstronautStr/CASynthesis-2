# Developer Questions Queue

Developer appends questions here when a design decision is needed.
User or Researcher answers in-line.

## Entry format
```
## [OPEN] YYYY-MM-DD — [Topic]
**Question:** ...
**Context:** what is being implemented and why this matters.
**Default choice:** what was done in the absence of an answer.
**Answer:** _(to be filled)_
```

Change `[OPEN]` to `[ANSWERED]` once resolved.

_Решённые треды (14 закрытых) вынесены в `archive/questions-resolved.md` — читать по
требованию. Последними при dream 2026-07-14 перенесены: explainer mode-toggle, explainer
excitation-секция, `dyn`, `tune` (все приняты Researcher по code-review; их слуховые
пласты отслеживаются в `current.md` «Ожидает прослушивания»)._

**Текущий статус 2026-09-15:** S/N реализованы, пользователь частично прослушал и оставил шесть заметок; результат отрицательный: похожее жужжание S, почти неизменный тон N. По его запросу выполнен [аудит математики и соответствия замыслу](research/audit-sn-2026-09-15.md): 15 записей / 45 дорожек пересчитываются точно, независимые формулы совпадают, полная регрессия проходит. Тональная PM-сеть следует [ТЗ](req-sonification-sn-demos-2026-09-14.md), но не проверяет богатую нелинейную динамику Gutter; это сужение постановки Researcher. S реагирует на правки в области чтения, некоторые клетки вне неё не влияют вовсе. Художественная приёмка не пройдена; новая модель и следующий REQ не согласованы. Дальнейший план — предложение в аудите.

**Последующее уточнение пользователя:** оба метода дают однообразное жужжание; Network вообще не даёт слышимого изменения звука. Формулировку выше «почти неизменный» не использовать для смягчения этого вердикта. Не пройдена базовая демонстрация слышимого эффекта, а не только художественная приёмка. Сравнение улучшений относительно Laplacian преждевременно; численная разница не считается прохождением этой ступени.

**Текущий статус N2, 2026-09-16:** реализация сверена с ТЗ, полная регрессия прошла; получен и [разобран фидбек по всем трём опытам](research/network-n2-review-2026-09-16.md). Б передаёт положение через окраску кликов, но даёт однообразный стук в такт поколений вместо ожидаемого разнообразия. В А пользователь отмечает разные высотные рисунки фигур и интересное движение тона глайдера. Ограничение представления событий подтверждено расчётом; выбранное продолжение N3 указано ниже. Осталось P2 к точности технического отчёта, ниже. [Актуальная передача](research/research-resume-2026-09-13.md).

**Следующий выбранный опыт, 2026-09-16:** по предложению пользователя подготовлено [ТЗ N3](req-network-combined-n3-2026-09-16.md): частотная настройка А + событийное возбуждение Б в одном тракте. Три опыта и готовые Notes; предварительные расчёты выполнены, реализации ещё нет. Конкретная схема сохраняет сеть Б и частоты А, добавляет затухающие банки по формулам ТЗ. Переход к геометрической среде пока отложен.

**Контекст 2026-09-13:** S7 сделан; пользователь уточнил, что глубоко не тестировал, и вернулся к [исследовательской постановке и пропоузалам](research/research-resume-2026-09-13.md). Пакетные запуски добавлять по потребности реального опыта; полезные признаки для автопоиска остаются открытым вопросом. Метрика «интересности» не согласована, локальность — возможный предмет исследования. Оба отложенных UX-вопроса ниже не решать; S8–S10 автоматически не начинать.

**Историческая проверка 10.09, до реализации S5:** при подготовке ТЗ часть подпроцессов получила ошибки памяти Windows/OpenBLAS; это не текущий статус принятого S5. Результаты реализации — в [логе S5](log/2026-09-12-demo-lab-s5.md).

## [ANSWERED] 2026-09-14 — Демо S/N: инженерные выборы по умолчанию (ревью Researcher 2026-09-15)

**Question:** Подтвердить или скорректировать пять выборов, которые ТЗ `req-sonification-sn-demos-2026-09-14.md` оставило разработчику.
**Context:** обе демки реализованы (`casynth_lab/scan_surface.py`, `casynth_lab/pm_network.py`, отчёт `log/2026-09-14-sn-demos.md`); ниже — места, где реализация выбирала сама.
**Default choice:**
1. Каталог разделён на два: `lab_catalog/sn_demos_2026_09_14/scan/` и `.../network/` (ТЗ называет один `sn_demos_2026_09_14/`). Причина: каждый ярлык открывает только свои записи, экран каталога показывает 12 строк — семь записей + сохранения пользователя не выталкивают демо-записи; в каталог добавлена прокрутка колёсиком.
2. Эволюционные сцены (F3 Pulsar): `rate_hz = 2` (в ТЗ не задано; прежние демо — 4). При 2 шагах/с цикл Pulsar = 1.5 с — эволюция W слышна как движение, а не как тремоло.
3. Уровни: критерий выравнивания — равный RMS с Laplace на F3 (12 с эволюции, с 1-й секунды); поправки Scan +6 dB, Network +3 dB стали значениями `trim_db` по умолчанию в реестре (ТЗ: «0 до подготовки постоянной поправки»). Фиксированный сырой запас — ×8 (+18 dB) до master gain в обоих движках.
4. Рендер S — аддитивный из K комплексных коэффициентов (точная band-limited оценка, без интерполяции таблицы и без внутренней повышенной частоты); «удвоенная плотность» проверяется на этапе чтения периода (8192 vs 16384), остаток < −50 dB.
5. F3 с эллипсом/Лиссажу (радиус 0.8) читает лишь хвосты сглаживания (|v − mean| < 0.05) — записей с этими путями на F3 нет; F3 только с Raster, случай перечислен в README и слуховом ТЗ. Kok's galaxy не добавлялся: Pulsar меняет W по циклу (max Δ ≈ 0.02) — резервный случай не потребовался.
**Answer (Researcher, 2026-09-15):** Проверено в [аудите](research/audit-sn-2026-09-15.md). Ответ относится к соответствию реализации ТЗ, не к слуховой приёмке.
1. Разделение каталога допустимо; все 7 S и 8 N записей доступны и точно пересчитываются.
2. `rate_hz=2` допустимо для первой пробы; утверждение «слышно как движение, а не тремоло» считать гипотезой, которую пользователь не подтвердил. Проверка показывает прежде всего слабое изменение связей, а не ошибку темпа.
3. Фиксированные ×8, +6/+3 dB согласуются с заданным способом постоянной калибровки. Скрытой нормировки каждого поля нет; проверка уровней подготовленных сцен и крайних сочетаний из тестов прошла. Это не доказательство субъективно равной громкости всех записей и не разрешение произвольного дополнительного усиления без проверки пиков.
4. Аддитивная оценка комплексного спектра реализует заданную полосу корректно; независимый DFT и расчёт волны совпадают. Сохранены фазы и непрерывность переходов в проверенных сценариях. Использование этого метода допустимо, оговорка про проверку переходных процессов и алиасинга остаётся.
5. Ограничение Ellipse/Lissajous на F3 описано верно; исключение почти пустого чтения из этих сравнений допустимо. Это также существенное ограничение свободного рисования, подтверждённое аудитом, а не свойство, которое следует замалчивать. Фраза выше «Kok's galaxy не добавлялся» устарела: фактически есть восьмая N-запись `sn_n_frozen_kok`. Малого, но ненулевого изменения W у Pulsar оказалось недостаточно для полезного слухового опыта; порог технического различия не считать художественной приёмкой.

## [DEFERRED] 2026-09-13 — UX: «Продолжить» и «Начать заново»

**Наблюдение пользователя:** кнопки воспринимаются как выполняющие почти одну и ту же задачу; различие между точным продолжением и новым запуском из поля не объясняет пользователю, когда нужна каждая.

**Решение:** зафиксировать вопрос и отложить разбор по прямой просьбе пользователя. S5 принят; интерфейс сейчас не менять. Объединение, скрытие или переименование кнопок не согласованы.

**Вернуться:** на запланированном разборе UX после первых 2–3 реальных исследовательских демо либо по отдельному запросу пользователя.

## [DEFERRED] 2026-09-13 — UX: переход стенда на готовый UI-пакет

**Наблюдение пользователя:** поле ввода в форме Save не вело себя как обычное виндовое (удержание Backspace, Ctrl+Backspace); вопрос — зачем изобретать велосипед, если есть готовые элементы интерфейса.

**Контекст:** в pygame виджетов нет; весь стенд (кнопки, слайдеры, вкладки, поле ввода) нарисован вручную. Точечно починено в `638d407` (автоповтор клавиш, Ctrl+Backspace). Готовые варианты: `pygame_gui` (полный тулкит: курсор, выделение, Ctrl+A/C/V, Home/End; новая зависимость в `requirements.txt` → входит в runtime-набор происхождения; свой цикл событий и тема — имеет смысл только для перевода ВСЕГО стенда, не одного поля), `pygame-textinput` (одно поле, Ctrl+Backspace нет), `tkinter.simpledialog` (родное поле, но второе окно с чужим циклом событий поверх pygame).

**Решение:** отложить; поле остаётся самодельным. Переводить стенд на `pygame_gui` целиком отдельным спринтом, если форм и текстового ввода станет больше (заметки, поиск по каталогу, редактирование названий).

**Вернуться:** на том же разборе UX после первых 2–3 реальных демо, вместе с вопросом двух кнопок.

---

## [SHELVED 2026-07-14] 2026-06-21 — REQ GoL-вокодер (T3): offline-прототип креативного ресинтеза
_Реквест Researcher → Developer. Дизайн — `decisions.md` 2026-06-21 «GoL-вокодер (T3)».
Новый offline-скрипт (напр. `gol_vocoder.py`), НЕ живой синт._

**Status (2026-07-14):** ОТЛОЖЕН решением Пользователя — «быстрый эксперимент, не хочу
на нём фокусироваться». Код `gol_vocoder.py` реализован и остаётся в репо; ревью
реализации и прослушивание НЕ проводились. Вернуться — по явному запросу Пользователя;
до тех пор тред не считается ни открытым (никто не должен его подхватывать), ни закрытым.

**Цель.** Креативный ресинтез: поиском найти бинарное GoL-поле, чей спектр прямого маппинга
приближает спектр целевого звука. Результат — эффект «цель сквозь GoL-металл», не точная копия.
Точная инверсия невозможна (коспектральность) — приближение есть характер.

**Требуется.**
1. **Переиспользовать `casynth_core.map_laplacian`** напрямую (это Python — НЕ дублировать
   движок, как в explainer; импортировать настоящий → паритет звука).
2. **Анализ цели.** Вход — WAV (моно-кадр стационарного участка). FFT → top-K пиков
   (частота+магнитуда). Отпечаток = **Bark-огибающая** (магнитуды по Bark-/лог-частотным
   бинам), якорь по низшему заметному партиалу = f0 (как `sel[0]` в ядре оставляет низшую
   моду = f0).
3. **Поиск — simulated annealing.** Состояние = бинарное поле фикс-окна (старт 8×8, размер —
   конфиг). Ход = флип случайной клетки (или малого блока). Eval кандидата: прогнать поле
   через `map_laplacian` (n/spread/alpha/shape из конфига) → (freqs,amps) → та же Bark-огибающая
   → дистанция `1 − cosine` к огибающей цели. Приём по Метрополису с температурным расписанием +
   **случайные рестарты** (держать глобально лучшее). По умолчанию **`shape>0`** (≈0.5–1), чтобы
   в матч входили и амплитуды, и n высокое (плотный спектр под цель).
4. **Выход.** Лучшее поле → (а) рендер WAV тем же движком; (б) сохранить как загружаемый
   паттерн (формат `patterns.py` / grid, чтобы можно было кинуть в `gol_synth.py` и при желании
   запустить эволюцию); (в) A/B-пара «цель vs результат» для прослушивания; (г) печать score+поля.
5. **Лог** (правило памяти): записать цель, найденное поле, score и ВСЕ параметры поиска/рендера
   (window, n, spread, alpha, shape, seed, расписание SA) — прогон воспроизводим.

**Критерии приёмки.**
- **Bring-up self-test (главный):** подать СОБСТВЕННЫЙ спектр известного поля (напр. blinker или
  случайного 8×8) как цель → SA восстанавливает поле с Bark-дистанцией ниже явного порога
  (в идеале → исходное поле или коспектральное ему). Доказывает, что петля анализ→поиск→рендер
  замкнута и метрика осмысленна.
- На синтетической цели (напр. гармонический стек) поиск **монотонно снижает** дистанцию
  относительно случайного старта; лучшее поле звучит как «цель сквозь металл», без NaN/ошибок.
- Рендер идёт **только** через `casynth_core` (никакого второго движка); смена `shape`/n/spread
  меняет результат согласованно с тем, как звучал бы синт на том же поле.
- Скрипт детерминирован при фиксированном seed; лог достаточен для повторного прогона.

**Default choice (если правки не будет):** прототипа нет — вокодер не послушать, ветка T3 закрыта.
**Answer:** _(не заполнен — тред отложен до возврата Пользователя)_

---

## [ANSWERED] 2026-06-22 — REQ Флудфилл-граф для Лапласиана (развилка extract 8×8)
_Реквест Developer → Researcher. Пользователь принял решение: флудфилл-граф делаем.
Нужен дизайн + критерии приёмки для реализации в `casynth_core.py`._

_Статус на 2026-07-14: дизайн дан и РЕАЛИЗОВАН (`fullshape`, реестровый Laplacian = ON);
тред держится здесь (не в архиве), потому что НЕ закрыт полностью: формального code-review
реализации ядра и слухового A/B «центр+shape vs вся форма» ещё не было (см. `current.md`
«Ожидает прослушивания»)._

**Контекст.** Текущий `_extract(patch, PATCH_SIZE=8)` вырезает окно 8×8 вокруг центроида
объекта и строит граф Лапласиана по этому патчу. Объекты крупнее 8×8 «слышатся» только
центром — периферия теряется. Сегментация (`ndimage.label`) при этом уже видит весь объект.
Кандидат (decisions.md 2026-06-17): строить граф Лапласиана напрямую по всем живым клеткам
связного объекта (результат `ndimage.label`), минуя 8×8 окно.

**Что нужно от Researcher (дизайн + REQ для Developer):**

1. **Контракт `map_laplacian`.** Как меняется сигнатура / входные данные?
   Сейчас: `map_laplacian(patch, f0, n, spread, alpha, shape, harm)` — принимает 8×8 патч.
   С флудфиллом Лапласиан должен работать на произвольном бинарном массиве (все клетки
   объекта). Это тот же `patch` увеличенного размера, или другой тип входа?

2. **Другие движки.** FFT/Walsh/Random/Granulo требуют регулярный патч фикс. размера.
   Как разрешаем несимметрию: флудфилл только для Лапласиана, остальные остаются на 8×8?
   Нужно ли новое поле в реестре `ENGINES` / другой контракт, или ветвление внутри
   вызывающего кода?

3. **Граница объекта.** Как передавать форму объекта в `map_laplacian`?
   `ndimage.label` даёт маску всего объекта. Кто извлекает маску — `casynth_core` или
   вызывающий код (`gol_life_synth_laplacian.py` / `gol_synth.py`)?

4. **Обработка крупных объектов.** Больше узлов → `eigvalsh` дороже; плотнее мод →
   вероятен рост roughness. Нужен ли потолок числа узлов (downsample/crop при превышении),
   или доверяем тому, что в рабочей зоне малых n шумный верх не проблема?

5. **Совместимость с текущими параметрами.** `spread/alpha/shape/harm` продолжают работать
   по-прежнему (отбор мод, амплитуды)? Или что-то из них меняет смысл при увеличенном графе?

6. **Обратная совместимость.** Нужна ли возможность переключиться обратно на 8×8 (флаг
   или отдельная функция), или старый код `_extract` можно просто выбросить?

7. **Тесты.** Какие инварианты проверять — позиция/поворот/отражение объекта в поле не
   меняют спектр (как сейчас), но теперь на произвольной маске, а не 8×8 патче.

**Default choice (если дизайн не придёт):** остаёмся на 8×8, крупные формы по-прежнему
слышатся только центром.
**Answer (Researcher, 2026-06-22): ПРИНЯТ дизайн ниже — полноценный REQ, готов к реализации.**
Дизайн-обоснование — `decisions.md` 2026-06-22 «Флудфилл-граф для Лапласиана».

---

### REQ Флудфилл-граф для Лапласиана (full-shape mode)

**Ключевая находка (она снимает половину вопросов).** Лапласов путь в `map_laplacian`
читает форму ТОЛЬКО через `np.argwhere(patch > 0)` + 8-окрестность по координатам
(строки 190–204). Он НИКОГДА не предполагает квадрат, 8×8 или регулярную решётку — граф
строится по живым клеткам как есть. То есть ядро УЖЕ умеет работать на произвольной маске;
единственный ограничитель — что вызывающий код кормит `extract(sub, 8)` (кроп до 8×8),
а не саму маску `sub`. И `sub` уже построен вызывающим кодом: в `gol_synth.py:371-372` и
`gol_life_synth_laplacian.py` каждый объект сегментируется (`ndimage.label`), и из него
делается ТОЧНЫЙ bbox-битмап `sub` (только клетки этого объекта). `extract(sub,8)` его потом
обрезает. **Флудфилл-граф = просто перестать обрезать: подать `sub` в Лапласиан напрямую.**

**Ответы на 7 вопросов.**

**1. Контракт `map_laplacian`.** Сигнатуру НЕ ломаем, добавляем флаг последним аргументом:
   `map_laplacian(patch, f0, n=..., spread=0.0, alpha=1.0, shape=0.0, harm=0.0, fullshape=False)`.
   - При `fullshape=False` (дефолт) — поведение бит-в-бит как сейчас (вход — 8×8 патч).
   - При `fullshape=True` — `patch` трактуется как **произвольный бинарный bbox-битмап одного
     объекта** (то, что вызывающий код кладёт в `sub`): любая высота×ширина, фон=0, объект=1.
   Внутри ядра НИКАКОЙ ветвёжки по `fullshape` НЕ нужно для самого построения графа — он уже
   агностичен к размеру. Флаг нужен только как явный СЕМАНТИЧЕСКИЙ переключатель «вход — патч
   vs полная маска» (чтобы реестр/вызов знали, что слать) и как точка крепления guard'а по
   числу узлов (см. п.4). Альтернатива «отдельная функция `map_laplacian_full`» отвергнута:
   тело идентично на 99%, дублирование = риск дивергенции (ровно то, против чего заведён
   `casynth_core`); один флаг чище.

**2. Другие движки — асимметрию разрешаем НА ВЫЗОВЕ, не в контракте каждого движка.**
   FFT/Walsh/Random/Granulo остаются строго на 8×8 (им нужна регулярная решётка фикс-размера;
   `_fft_order`/`_walsh_order`/`_R_MAT` прекомпьютены под `PATCH_SIZE`). НЕ делать `fullshape`
   общим параметром всех движков. Маркер в реестре `ENGINES`: добавить булев ключ движка
   `fullshape=True` ТОЛЬКО Лапласиану (остальным — отсутствует/False). Вызывающий код ветвит:
   ```
   spec = ENGINE_BY_ID[engine_id]
   shape_in = sub if spec.get('fullshape') else extract(sub, PATCH_SIZE)
   freqs, amps = fn(shape_in, f0, **kwargs)   # kwargs включает fullshape=spec.get('fullshape', False)
   ```
   `sub` у вызывающего уже есть — лишних вычислений ноль; `extract` зовётся лениво только для
   гармонических. Это инженерия вызывающего кода (`gol_synth.py`) — НЕ под дизайн-ревью; здесь
   фиксирую лишь контракт, который ядро обязано поддержать (флаг + маркер реестра).

**3. Границу объекта извлекает ВЫЗЫВАЮЩИЙ код — он это УЖЕ делает.** Маска `sub`
   (`gol_synth.py:369-372`) — тот самый флудфилл-результат `ndimage.label` в тайтовом bbox.
   `casynth_core` НЕ трогает `ndimage.label` и НЕ занимается сегментацией — это разделение
   ответственности соблюдается и сейчас (ядро получает уже изолированную форму). С флудфиллом
   ничего не меняется: вызывающий просто перестаёт кропать `sub` перед подачей в Лапласиан.

**4. Крупные объекты — потолок узлов ОБЯЗАТЕЛЕН (`eigh` это O(V³)).** В full-shape добавить
   константу `MAX_LAPLACIAN_NODES` (старт **256** — детерминированный потолок, ниже tunables).
   Если число живых клеток объекта > потолка — **детерминированный даунсэмпл маски** до
   укладки в потолок ПЕРЕД построением графа: децимация решётки целым шагом
   `step = ceil(sqrt(V / MAX))` (`mask[::step, ::step]`), затем при необходимости добор —
   но проще и достаточно фиксированной децимации по шагу; шаг детерминирован от `V` →
   воспроизводимо. ВАЖНО: даунсэмпл должен оставаться инвариантным к позиции (децимация от
   bbox-origin маски, не от позиции в поле — `sub` уже origin-relative, инвариантность держится).
   Перф-замечание: при `shape=0`/`harm=0` и малом `n` мы всё равно считаем ВЕСЬ спектр
   (`eigvalsh`), берём низшие — потолок 256 держит `eigvalsh` в ~единицы мс, приемлемо для
   объектов Life (они редко >256 клеток). Шумный верх плотного спектра — НЕ проблема в рабочей
   зоне (берём мало низших/разнесённых мод, decisions.md 2026-06-17); потолок здесь про
   стоимость `eigh`, не про тембр.

**5. Совместимость `spread/alpha/shape/harm` — смысл СОХРАНЯЕТСЯ, оси работают как есть.**
   Все четыре оперируют ПОСЛЕ построения спектра, над `mode_freqs`/собств.векторами/амплитудами —
   они агностичны к тому, граф 8×8 или флудфилл. Нюансы (НЕ требуют кода, для прослушивания):
   - `spread`: на крупном графе спектр длиннее → `spread=1` дотягивается ВЫШЕ (больше «материала»
     наверху). Это и есть желаемый эффект флудфилла — периферия формы теперь слышна. Низшая
     выбранная = f0 держится (`sel[0]=0`).
   - `shape`: краевое возбуждение `e=deg` теперь видит ВСЮ границу объекта, а не кромку 8×8-кропа
     → ровно цель развилки (крупные формы различаются периферией). `|⟨e,φ_k⟩|` инвариантно к
     изоморфизму графа на маске любого размера.
   - `harm`: квантование отношений `r_k` — поэлементная ф-я, безразлична к размеру.
   - `alpha`: rolloff `1/i^α` по номеру выбранной моды — без изменений.
   НИЧЕГО из них не меняет смысл; full-shape лишь даёт им более богатый спектр на вход.

**6. Обратная совместимость — `extract` НЕ выбрасывать, `fullshape=False` НЕ удалять.**
   Причины: (а) гармонические движки ОБЯЗАНЫ остаться на `extract`/8×8 (п.2) — функция нужна;
   (б) дефолт `fullshape=False` = бит-в-бит исторический Лапласиан → все baseline-тесты держатся
   без правок (load-bearing для процедуры ревью); (в) 8×8-режим — валидная A/B-точка для
   прослушивания «центр vs вся форма» (Пользователь сравнивает на слух — без отката сравнить
   нельзя). Переключатель: новый Laplacian-движок с `fullshape=True` ЛИБО держать оба как
   варианты. Рекомендация: **сделать `fullshape=True` дефолтом РЕЕСТРОВОГО Laplacian-движка**
   (это то поведение, которое хотим слышать), но ядро дефолтит `fullshape=False` (бит-в-бит для
   тестов). То есть: ядро консервативно, реестр прогрессивен. Если на слух full-shape окажется
   хуже на каких-то режимах — откат = снять маркер в реестре, код ядра не трогаем.

**7. Тесты — инварианты те же, но на ПРОИЗВОЛЬНОЙ маске (не 8×8).**

**Критерии приёмки.**
- **A. fullshape=False → бит-в-бит.** ВСЕ существующие тесты Лапласиана
  (`..._backward_compatible`, `..._lowest_mode_is_f0`, `..._uses_sqrt_lambda`, shape-тесты,
  harm-тесты, `..._invariant_to_rotation_reflection_translation`) проходят БЕЗ изменений.
  Не править их. Дефолт `fullshape=False` = исторический путь.
- **B. fullshape на ≤8×8 форме = тот же спектр, что 8×8-путь.** Для формы, целиком влезающей
  в 8×8 (напр. `_ell_patch`, `_blinker_patch`), `map_laplacian(sub, f0, fullshape=True)` даёт
  ТЕ ЖЕ sorted-freqs и sorted-amps (atol 1e-6), что `map_laplacian(extract(sub,8), f0)` —
  где `sub` = тайтовый bbox той же формы. Доказывает, что full-режим — не новый приём, а
  снятие кропа: на формах, которые кроп НЕ резал, результат идентичен. (Тонкость: центрирование
  в 8×8 vs тайтовый bbox дают РАЗНЫЕ массивы, но ОДИН граф живых клеток → один спектр. Если
  тест падает — значит ядро где-то зависит от размера массива/фона; это баг, лови его.)
- **C. Крупная форма звучит ПЕРИФЕРИЕЙ (главный тест развилки).** Форма >8×8 (напр.
  L-образная 12×12 с длинными плечами): `map_laplacian(..., fullshape=True)` даёт спектр,
  ОТЛИЧНЫЙ от 8×8-кропа той же формы (sorted-freqs различаются заметно — не atol-равны).
  Доказывает, что периферия теперь влияет на звук. (Дополнительно при shape=1: амплитуды
  full-shape ≠ амплитудам 8×8-кропа — краевое возбуждение видит всю границу.)
- **D. Инвариантность на произвольной маске.** На асимметричной форме >8×8 sorted-freqs
  (и при shape=1 sorted-amps) при `fullshape=True` совпадают (atol 1e-6) для маски и её
  rot90/rot180/fliplr/flipud. (Трансляцию НЕ тестируем массивом — `sub` уже origin-relative;
  вместо неё проверить, что добавление пустых рамок фона вокруг маски НЕ меняет спектр:
  фон=0 не даёт узлов графа.)
- **E. Привязка f0 при full-shape.** `freqs[0] == f0` (atol 1e-6) при `fullshape=True` для
  любых spread/shape/harm∈{0,0.5,1} (как сейчас — `sel[0]=0`, `nonzero_sq[0]→f0`).
- **F. Потолок узлов детерминирован и инвариантен.** Форма с V>`MAX_LAPLACIAN_NODES` живых
  клеток: (1) `eigh`/`eigvalsh` вызывается на ≤MAX узлах (не падает по памяти/времени —
  можно проверить на синтетическом плотном блоке, напр. 20×20=400>256); (2) результат
  детерминирован (два вызова на той же форме идентичны); (3) даунсэмпл инвариантен к
  rot90/flip большой формы (sorted-freqs совпадают atol — допускается чуть бо́льший atol,
  т.к. децимация может слегка нарушить точную симметрию; если строгий 1e-6 не держится,
  ослабить до 1e-3 И зафиксировать это в тесте как известный эффект децимации).
- **G. Контракт/реестр.** `test_contract_shapes_and_ranges`, `test_empty_patch_is_silent`,
  `test_engine_registry_integrity`, `..._call_with_defaults` — зелёные. Пустая/одноклеточная
  маска при `fullshape=True` молчит (`cnt<2` → нули, как сейчас). Реестр: Laplacian имеет
  маркер `fullshape=True`; `test_engine_registry_call_with_defaults` зовёт `fn` корректно
  (передаёт `fullshape` если движок его объявляет — иначе дефолт ядра `False`).

**Не делать (out of scope — отдельные развилки/инженерия).**
- НЕ менять контракт гармонических движков (FFT/Walsh/Random/Granulo остаются 8×8).
- НЕ переносить ветвёжку `sub vs extract` в ядро — это инженерия вызывающего кода
  (`gol_synth.py`), под дизайн-ревью не попадает; ядро лишь обязано принять флаг + маркер.
- НЕ менять алгоритм даунсэмпла на «умный» (морфологический skeleton, медиана и т.п.) —
  простая решёточная децимация по шагу достаточна для держания стоимости `eigh`; умный
  ресемпл — отдельная дверь, только если на слух потолок режет характер крупных форм.
- НЕ трогать `harm`/`shape`/`spread`/`alpha` (работают как есть на любом размере графа).
- Взвешенный Лапласиан (диагональ 1/√2, decisions.md 2026-06-16) — отдельный приём, не сюда.

---

_Прежние треды решены. Последний — «Живые крутилки тембра Laplace» — реализован Developer
(2026-06-16) и принят Researcher (2026-06-17): DEV-2 закрыт (tail-pool), DEV-1 в режиме
ручного headroom. Перенесён в архив. Алгоритмы и реестр `ENGINES` в `casynth_core.py`
приняты Researcher 2026-06-17; прототип `gol_synth.py` — инженерия, не под дизайн-ревью
(см. `current.md` «Статус ревью»)._

## [ANSWERED] 2026-09-15 — N0: условия переноса в N1; точность Max-обвязки не установлена
**Question:** Согласовать четыре допущения, которые нельзя установить из файлов патча без запуска Max; менять ли их до N1?
1. **Задержка обратной связи через `send~`/`receive~`.** В петле узел→`p matrix`→узел две пары send~/receive~; Max добавляет минимум один вектор сигнала (по документации — только там, где нужно разорвать петлю). Принято: `EXTRA_FEEDBACK_SAMPLES = 64` (sigvs по умолчанию) поверх `delay~` 2000 → полная задержка связи 2064 отсчёта. Возможные варианты: 0 / 64 / 128 и другой sigvs пользователя автора.
2. **`delay~` времени связи.** Патч при загрузке задаёт `2000 + random(2000)` отсчётов (невоспроизводимо). Зафиксировано 2000 (значение-аргумент `delay~ 8000 2000`).
3. **`svf~ 20` / `svf~ 30` (HP-выходы) после узла.** Точная реализация Max не опубликована; взят SVF Чемберлина, `f = 2 sin(pi fc/sr)`, `q = 1 - res`, res 0.01. При 20/30 Гц влияет только на субнизкую полосу, но эта полоса как раз заметна в режимах с низким демпфированием.
4. **Порядок восстановления UI при загрузке.** Значения слайдеров узла (`autopattr restore` в mxjGutterCompact: gain 162, damp 138, mod 46, rate 39, Q 149, soften 103) взяты за исходные. Главный патч при загрузке тоже рассылает свои (gain 150, damp 159, mod 46, rate 23, Q 149) и пресет движения SLOW (случайные mod/rate по узлам); фильтры узлов рандомизируются через 1 с после загрузки (формула источника, у нас — с фиксированным seed). Какой набор оказывается последним — без запуска Max не установить.
**Context:** REQ N0 требует решить семантику обратных соединений до утверждения соответствия сети. Узел (`gutterOsc.class`) сверен побитово с оригиналом на JVM; неопределённость — только в Max-обвязке. Все четыре пункта вынесены в константы `config.json` пакета и меняются без правки кода.
**Default choice:** как описано выше; зафиксировано в `demos/results/network_reference_n0/manifest.json` и README.
**Answer (Researcher, 2026-09-15):** Для [N1](req-network-ca-n1-2026-09-15.md) фиксируем конкретную модель; полного соответствия запущенному Max не утверждаем.
1. Оставить дополнительную задержку 64, полную — 2064 отсчёта. Чтение и направление этого маршрута проверены независимо, ошибка 0. Это выбранное условие N1, не установленная семантика планировщика Max.
2. Оставить общий `delay=2000`, без нового случайного выбора. Значение и версия конфигурации входят в запись опыта.
3. Оставить используемые SVF 20/30 Гц с `q=0.99` как явное приближение. Проверка `q=1.98` не разрушила частотный опыт N1, но не подтверждает оригинальную формулу.
4. Использовать значения узлов и полный список частот из [fixtures](research/network-n1-fixtures-2026-09-15.json). Это фиксированная инициализация опыта; не реконструированный порядок загрузки Max и не новый вызов генератора случайных частот.

Перед N1 исправить доказанное расхождение: `R[5]=0` до мастер-суммы и матрицы. В исходном `p individual_controls` правый выход шестого узла не подключён. Дополнительно уточнить генератор частот: в пакете три значения выше заявленных 5000 Гц, поведение исходного multislider на входах вне [0,1] не проверено. Эти пункты и границы выводов о гистерезисе разобраны в [ревью](research/audit-network-n0-2026-09-15.md). Пользователь не должен разрешать технические неопределённости обвязки для продолжения работы.

## [ANSWERED] 2026-09-16 — N2: сбросы узлов Gutter (сторона А) в стресс-пробе «всё поле каждый блок»
**Question:** Считать ли reset'ы Duffing-узлов Gutter при переключении всего поля каждый аудиоблок
(125 Гц перестройки всех банков) дефектом поставки N2 или свойством модели N0/N1 вне штатного диапазона?
**Context:** ТЗ N2 требует «без NaN, клиппинга и аварийных reset» в проверках уровня (плотные/случайные
поля, повторные ручные правки, концы ручки «Отклик»). В `demos/n2_events_report.py` все эти режимы
чистые на обеих сторонах. Дополнительная проба preflight для Б (`full_toggle_every_block`, rho 0.60/0.95,
gain 0.04) на Б даёт пик 0.969 без клипа (preflight 0.972), но на А — ~9·10⁵ reset'ов за 6 с
(NaN-защита `resetDuff()` узла gutterOsc). Исходный N1 `gutter_field` под тем же стимулом сбрасывается
ИДЕНТИЧНО (1044547 за 6 с); при переключении раз в 4 блока (32 мс) и реже — 0 reset'ов у обоих.
Периодическое чтение и trim здесь ни при чём; руками/КА такой стимул недостижим.
**Default choice:** проба оставлена в отчёте как наблюдение (`observations`), не как провал поставки;
контрольные числа N1 приложены. Модель А не менялась.
**Answer (Researcher, 2026-09-16):** Это воспроизводимое ограничение исходной модели А; для технической сверки трёх поставленных сцен оно не блокирует разбор, но общей устойчивости А не подтверждаем. В независимой пробе исходный и периодический Gutter дали одинаковые 1 047 363 сброса за 6 с при полном переключении каждый блок; при интервалах 2/3/4/8/16 блоков сбросов нет. Формулировка «недостижимо» не обоснована: API допускает такой поток команд. Модель А ради этой пробы не менять. Итог отчёта исправить по замечанию ниже. Слуховой результат пользователя уже получен и не считается принятым по факту технического соответствия: [ревью](research/network-n2-review-2026-09-16.md).

## [OPEN] 2026-09-16 — N2 P2: итог отчёта скрывает сбросы стресс-пробы
**Question:** Исправить формирование итогового статуса `demos/n2_events_report.py`.
**Context:** `observations` содержит сбросы узлов А, но `summary` утверждает `no resets`; это противоречит таблице измерений. Ограничение модели рассмотрено Researcher выше.
**Answer (Researcher, 2026-09-16):** Разделить итог готовых сцен и стресс-проб; при ненулевых сбросах явно перечислить их и область применимости результата. Критерий: повторно сгенерированные JSON/Markdown не содержат безусловного `no resets`, если в измерениях есть сбросы. Звук и пользовательские Notes не менять. Замечание к достоверности отчёта, не новая звуковая гипотеза.

## [OPEN] 2026-09-16 — N3: клип в стресс-пробе «всё поле каждый блок» при Decay 1.5 с
**Question:** Нужна ли поправка общего коэффициента выхода N3 (сейчас ×4) или это ограничение стресс-пробы, не сцен?
**Context:** ТЗ N3 требует в стресс-пробах при gain 0.04 «finite, без клиппинга». Всё выполнено, кроме одной пробы: полное переключение поля каждый блок (125 Гц) при `field_tuning=1` и T60 1.5 с — пик до клипа 1.157, ~100 блоков из 750 с клипом (T60 1.2 → 1.051; 1.0 → 1.007; дефолт 0.8 → без клипа; Fixed → 0.987, без клипа). Preflight давал 0.735 для этой пробы без шагов КА; в отчёте КА идёт (6 gen/s) поверх переключения. Готовые сцены при gain 0.028: пики 0.054…0.105 (запас > 19 dB), RMS совпадают с preflight до 0.1 dB. Коэффициент по ТЗ менять только по согласованию.
**Default choice:** ×4 оставлен; клип отмечен в итоге отчёта `demos/results/network_n3/report.md` как ограничение стресс-пробы. Если нужен запас — ×3.4 (−1.4 dB) убирает клип при T60 1.5 в этой пробе; менять надо до прослушивания, с пересборкой каталога.
**Answer:** _(Researcher / Пользователь)_

## [ANSWERED] 2026-09-16 — N4: следующее задание Developer

**Question:** Какой следующий опыт реализовать после первого слухового результата N3?
**Answer (Researcher, 2026-09-16):** Пользователь выбрал резонаторы отдельных фигур с частотами лапласиана и круговыми детекторами, захватывающими соседей. [ТЗ N4](req-object-resonators-n4-2026-09-16.md) готово: отдельный движок, динамические цвета/круги/центры, два живых опыта с гипотезами и проверенными исходными клетками. Прямое условие пользователя — не сломать существующий лапласиановый синт; обязательны старые тесты и неизменный PCM-эталон. Численная предварительная проверка не заменяет реализацию, полное техническое ревью и прослушивание. Готовый каталог сдаётся Researcher до предъявления пользователю.

## [ANSWERED] 2026-09-16 — N4: выходной коэффициент ×0.5, клип и hard drops в стресс-пробах, стоимость анализа больших фигур
**Question:** (а) Оставить ли общий выходной коэффициент N4 ×0.5 (калибровка preflight), при котором готовые сцены дают пики ≤ 0.09, а стресс-пробы при gain 0.04 (vol 1) клипуют? (б) Считать ли hard drops хвостов при замене всего поля каждый блок и превышение бюджета блока анализом фигур > ~300 клеток ограничениями первой версии или дефектами поставки?
**Context:** ТЗ N4 запрещает AGC и деление на число фигур; единственный общий рычаг — ×0.5. Сцены: N4.1 Б −40.1 dBFS / пик 0.056, N4.2 −35.6 / −35.2 dBFS / пики 0.082–0.086 (== preflight до 0.02 dB); N4.1 А (N3 Tuned) на 2.25 dB тише Б при том же gain стенда — параметра сцены, действующего на одну сторону, нет. Стресс при 0.04: плотное случайное поле и случайное поле каждый блок при scale 55 / decay 0.2 — пики до клипа > 1 (до 24 банков бьются одновременно с a≈1); замена всего поля каждый блок → до 24 новых хвостов за блок, пул затухающих (24) переполняется, самые тихие обнуляются сразу (счётчик `dropped` виден в панели; в сценах 0). Анализ (eigvalsh L): 256 клеток 7 ms, 529 — 37 ms, 1024 — 101 ms на render-потоке при каждом изменении поля (в сценах фигуры ≤ 17 клеток, p99 обеих сторон ≈ 1.0–1.5 ms). Числа — `demos/results/object_resonators_n4/report.md`.
**Default choice:** ×0.5 оставлен (числа preflight воспроизведены); клип/дропы/бюджет отражены как ограничения стресс-проб в отчёте, не как провал сцен. Если нужен запас — общий коэффициент менять до прослушивания с пересборкой каталога; для больших фигур возможен разреженный решатель (eigsh с фиксированным v0) как отдельная задача.
**Answer (Researcher, 2026-09-17):** Для двух уже поставленных сцен ×0.5 оставить: независимо подтверждены пики ≤0.086, точное воспроизведение и бюджет блока. Это не калибровка для любых полей: отдельная проба хвостов даёт пик 2.565, не 1.219. Превышение бюджета большими фигурами — явно указанное ограничение первой пробы, не повод молча упростить граф; новый асинхронный/разреженный тракт сейчас не заказывался. Мгновенный обрыв вытесняемого хвоста нарушает требование короткого перехода и остаётся P2. Полная приёмка N4 не закрыта: [ревью и три замечания](research/object-resonators-n4-review-2026-09-16.md). Для заказанного ниже режима Objects / Laplace с другими весами провести новую постоянную калибровку; прежнюю ×0.5 на него автоматически не переносить.

## [OPEN] 2026-09-17 — Objects: сравнение с Laplace и его настройки

**Question:** Следующее задание после N4 и добавленного Radius x.
**Answer (Researcher, 2026-09-17):** Прямой запрос пользователя: А/Б сравнивать прежде всего со старым Laplace, в Objects нужны его настройки для сравнения «чистого» и «апгрейднутого». [ТЗ](req-objects-laplace-comparison-2026-09-17.md): режим спектра Laplace со всеми семью параметрами и общей f0; прежний спектральный путь N4 доступен как Figure; три живых пары Laplace/Objects, выверенные клетки и гипотезы, постоянное выравнивание уровней. [Preflight](research/object-resonators-laplace-preflight-2026-09-17.json) подтверждает геометрию сцен, действие всех семи ручек за период Galaxy и переключение приёма соседа радиусом ×1/×1.5 без выключения блинкера. До сдачи закрыть три P2 ревью (возврат мод, обрывы хвостов, сводка пиков), пройти старые гейты. Реализации нового режима и новых пар пока нет; сохранить прежний каталог и Notes. Сначала техническая сверка, затем слуховой результат.

## [ANSWERED] 2026-09-17 — Objects / Laplace: константа режима 0.7 и side_gain на сцену; `n` в фазах 3/7 Galaxy
**Question:** (а) Принимается ли способ выравнивания: одна константа режима Laplace `LAPLACE_GAIN = 0.7` (веса = 0.7 × амплитуды `map_laplacian`, без нормировки на число мод/фигур), подобранная по среднему разрыву RMS трёх сцен, плюс постоянный `audio.side_gain` стороны Б на опыт (1.08 / 0.74 / 1.25 → А−Б ≤ 0.03 dB)? (б) Preflight утверждает, что `n` меняет спектральные данные Galaxy во всех 8 фазах; в фазах 3 и 7 все компоненты — 4 клетки (3 моды), n=4 и n=12 выбирают одни и те же три моды — совпадают и частоты, и веса. Это артефакт сравнения дополненных нулями массивов старого синта; в отчёте и тестах ожидание `n` = фазы [0,1,2,4,5,6].
**Context:** ТЗ разрешает и общую калибровку режима, и минимальную калибровку стороны в стенде (default 1, старые записи побайтно) — реализованы обе; выравнивание одним `LAPLACE_GAIN` без side_gain дало бы разброс −2.6…+1.9 dB между сценами.
**Default choice:** (а) как описано; (б) ожидание по фактическим данным, preflight не правился.
**Answer (Researcher, 2026-09-17):** (а) Соответствует разрешённой в ТЗ постоянной калибровке режима и стороны. Она не является AGC и не меняется при игре. (б) Верно: мой preflight сравнил в том числе длины массивов с нулевым дополнением. Для звучащих мод `n=4` и `n=12` в фазах 3/7 ничего не меняют; исправлены данные preflight, правильные фазы [0,1,2,4,5,6]. Целевые 13 тестов Objects/Laplace независимо прошли. Это ответ на два технических вопроса, не объявление полного независимого ревью поставки. Получен положительный слуховой фидбек пользователя; следующий запрос ниже.

## [ANSWERED] 2026-09-17 — Objects: ручной диапазон радиуса, соседние события и Attack

**Question:** Следующее задание по результатам игры пользователя с Objects/Laplace.
**Answer (Researcher, 2026-09-17):** Пользователю интересны арпеджиаторные рисунки и полиритмы, хочет остаться на прототипе. [Фидбек](research/objects-feedback-radius-attack-2026-09-17.md) содержит настройки скриншота, проверку приёма чужих событий и различение возбуждения/клипа/underruns. [ТЗ](req-objects-radius-attack-2026-09-17.md): Min/Max у Radius x (начальные 0.25/4), произвольные конечные неотрицательные границы, линейный слайдер; Attack 0…20 ms, default 0 сохраняет текущий импульс; две радиусные пары (×1/×2 и ×1/×32) и одна пара атаки (0/4 ms). [Preflight](research/objects-radius-attack-preflight-2026-09-17.json) подтверждает конкретные поля и звук радиусных пар; атака пока только скалярная проба. Источник события — чужая изменившаяся клетка внутри круга; перенос чужих частот в банк не вводить. Новые ручки/каталог пока не реализованы. Существующие пользовательские записи и Notes сохранить.

**Результат (Researcher, позднее 2026-09-17):** Реализовано; прочитаны три новых Notes. [Разбор](research/objects-radius-attack-feedback-2026-09-17.md): R1 — дополнительный отклик слышен, R2 — различий пользователь не услышал, A1 — работа атаки подтверждена. Все девять дорожек независимо точно пересчитаны. Общий приём в R2 работает, но преимущественно меняет силы уже имеющихся импульсов при тех же спектральных последовательностях. Численная разница не опровергает отрицательный слуховой результат. Нового задания на изменение модели из этого не следует.

## [OPEN] 2026-09-17 — Radius range / Attack: поле пресета U0 и диапазоны при копировании сторон
**Question:** (а) ТЗ просит сохранить настройку пользователя Radius 0.34 отдельным пресетом, но не задаёт поле.
Взято поле R2/A1 (Jam p3 + Octagon II p5 — полиритм, отмеченный пользователем), обе стороны одинаковы, диапазон
слайдера 0.25…1. Годится ли, или пресет нужен на другом поле (например, на поле одной из пользовательских
записей)? (б) `copy_side` / `factory` / `copy_spectrum` диапазоны Min/Max не переносят и не сбрасывают
(диапазон — состояние UI стороны, не звук); значение вне диапазона после копии стоит у края слайдера.
Переносить ли диапазон вместе с полной копией стороны?
**Context:** `memory/log/2026-09-17-objects-radius-attack.md`; каталог `lab_catalog/objects_radius_attack_2026_09_17/`.
**Default choice:** (а) поле R2/A1; (б) не переносить.
**Answer:** _(Researcher / Пользователь)_

## [OPEN] 2026-09-17 — Objects: Births / Deaths и возбуждение по месту рождения

**Question:** Пользователь поручил два опыта: А/Б только рождения / только смерти и избирательное возбуждение мод разными участками фигуры.
**Answer (Researcher, 2026-09-17):** [ТЗ](req-objects-event-source-modal-2026-09-17.md), [поля и фазовые расчёты](research/objects-event-source-modal-preflight-2026-09-17.json). E1 — Octagon II p5 с разными паузами у рождений и смертей; M1 — Jam p3, Births на обеих сторонах, общий / пространственно распределённый импульс. Формула первой пространственной пробы — положительные веса по квадратам участия клеток в модах; детали — постановка Researcher. Исходный Both был выбором Researcher, не пользователя. Реализации и слухового результата новых опытов пока нет.

## [OPEN] 2026-09-17 — Events / Birth position: три инженерных выбора Developer
**Question:** (а) Стартовое заполнение поля (init, поле против нулей) в Births — пакет из всех клеток фигуры; в
Birth position при этом все узлы рождены → b = 1 (общий удар). В Deaths стартового пакета нет: первый звук Б в E1 —
на первом переходе со смертями. Годится ли такой старт для сравнения (участок до 2 с в калибровке не учитывался)?
(б) При split/merge в Births + Birth position новые фигуры получают пространственный пакет по своим новым
клеткам (как и любая новая фигура: B = её рождения в K_cur); старые банки уходят в хвост без пакета. (в) Хвост
Deaths при полностью занятых пулах хвостов/затуханий затухает на месте за 20 мс вместе с последним ударом
(прежнее правило «in place», счётчик) — отдельного исключения для последнего удара не сделано. (г) Birth position
вне сочетания Births+Own+Laplace+full: кнопки остаются доступны, движок не делает пакетов и показывает условие
красным (`unsupported_packets`); подмены общим ударом нет — но тишина. Альтернатива — блокировать кнопку через
`inactive` (тогда нельзя вернуться на Uniform, не сменив другие ручки).
**Context:** `memory/log/2026-09-17-objects-event-source-modal.md`; каталог `lab_catalog/objects_event_source_modal_2026_09_17/`.
**Default choice:** (а) как описано; (б) пространственный пакет новым фигурам; (в) прежнее правило in place;
(г) кнопки доступны + красная строка условия.
**Answer:** _(Researcher / Пользователь)_

## [OPEN] 2026-09-18 — Objects: сила влияния места рождения, M2

**Question:** Пользователь поручил добавить регулируемую силу Birth position и попробовать усиленное влияние на прежнем Jam.
**Answer (Researcher, 2026-09-18):** Раздел 5 [ТЗ](req-objects-event-source-modal-2026-09-17.md) готов к реализации. Birth strength 0…4 (default 1): 0 = Uniform, 1 = прежний Birth position, выше 1 — усиленная избирательность. Полный закон, нулевое участие, сохранение истории пакетов и совместимость заданы явно; [preflight](research/objects-birth-strength-preflight-2026-09-18.json) проверяет преобразование всех фаз M1. Один новый живой M2: А=1, Б=4, тот же Jam и прежние настройки, свободная ручка, новая постоянная калибровка; гипотеза в ТЗ. Каталог `lab_catalog/objects_birth_strength_2026_09_18/`, вход `run_objects_birth_strength.bat`. Реализации и слухового результата M2 ещё нет. Disk и короткий Decay остаются отдельными предложениями.

**Результат (Researcher, позднее 2026-09-18):** Реализовано; [фидбек пользователя из разговора](research/objects-birth-strength-feedback-2026-09-18.md): усиление почти ничего не даёт, слышен слабый уход части составляющих, общее звучание прежнее. Гипотеза полезного усиления различий не подтвердилась. Чтение кода исключает перемножение радиуса и новой ручки в M2: Own не использует радиус. Направление изменения окраски согласуется со степенным отбором мод; причина слабой слышимости отдельно не установлена. Это не полная независимая техническая приёмка; нового задания Developer нет.

## [OPEN] 2026-09-18 — Objects: затухание от истории клеток, D1–D3

**Question:** Пользователь поручил ТЗ опытов с затуханием, которые информируют следующий исследовательский выбор.
**Answer (Researcher, 2026-09-18):** [ТЗ](req-objects-decay-2026-09-18.md) готово. Fixed / Common age / Modal age; свежесть живой клетки убывает по аудиочасам, новые клетки свежие, история задаёт времена затухания выбранных мод. D1 — Octagon p5, Fixed против общей меняющейся скорости потерь; D2 — Blinker, общие против раздельных потерь с двумя паузами для хвостов; D3 — Jam p3 + Octagon p5, прежний звук против полного нового механизма. [Preflight](research/objects-decay-preflight-2026-09-18.json) задаёт клетки, блоковые часы, фазовые q/T; эффекта в интегрированном аудио ещё не проверяет. В ТЗ есть Notes и таблица выводов по исходам, работа с хвостами/памятью/совместимостью и технические гейты. Каталог `lab_catalog/objects_decay_2026_09_18/`, вход `run_objects_decay.bat`. Реализации и слухового результата ещё нет.

**Реализация (Developer, 2026-09-18):** сделано — [лог](log/2026-09-18-objects-decay.md), [отчёт измерений](../demos/results/objects_decay/report.md); каталог из трёх записей запинен на `1fdf8d1`, Notes = гипотезы дословно. Слухового результата нет.

## [OPEN] 2026-09-18 — Decay law: инженерные выборы реализации и одно наблюдение (для сверки Researcher)

**Question:** Подтвердить или поправить выборы, которые ТЗ оставило разработчику, и учесть наблюдение по D1 при оценке результата.
**Context:** Objects v6 (`ca_object_resonators_n4_v6`), опыты D1–D3. Все технические критерии раздела 5 сняты в отчёте; ниже — места, где реализация выбирала сама.
**Default choice:**
1. Паузы D2 заданы новым необязательным ключом сцены `script`: раннер ставит их при каждом старте сцены с начала (Start, отпускание паузы остановленной сцены, Restart). Так «живое открытие с начала подаёт тот же сценарий», а Continue с конца их не повторяет. В журнале записи паузы есть, с пометкой `script`; реплей каталога их пропускает, потому что воспроизведённый старт ставит их снова. Старые сцены и записи этого ключа не имеют.
2. Хвост, отделившийся при Fixed, при переключении на Common/Modal фиксирует Decay момента переключения. Дальнейшие движения Decay его не трогают: «отделённые хвосты заканчиваются по сохранённому закону». Пока закон остаётся Fixed, хвосты по-прежнему следуют ручке Decay.
3. Кнопки закона подписаны Fixed / Common / Modal: три кнопки по 60 px, полные имена не помещаются. Полные имена стоят в шапке панели («Modal age  stable 1.39 s;  rows: target T») и в `display`. Строка фигуры при адаптивном законе показывает целевой диапазон T мод вместо числа a.
4. Недопустимое сочетание блокируется в обе стороны. При Figure или 8x8 кнопки закона заменены текстом с причиной. При Common/Modal заблокированы Spectrum и full: сначала нужно вернуть Fixed. Раннер отвергает такую команду ещё при постановке.
**Observation (D1):** свежесть убывает и между поколениями (tau 0.5 с при шаге 0.5 с). Поэтому средняя применённая T хвоста за интервал фазы — 0.96 / 0.91 / 0.73 / 1.23 / 1.05 с — заметно длиннее цели в момент шага (0.73 / 0.65 / 0.41 / 1.15 / 0.85 с). Спад в PCM стороны Б следует применённой T в пределах 3 %: 0.97 / 0.92 / 0.71 / — / 1.06 с. Сторона А держит 0.94 с. Контраст фаз в звуке около ×1.5–1.7, а не ×2.8, как у чисел на шагах. Это свойство закона («мгновенное эквивалентное T»), не ошибка реализации. Если пользователь скажет «Б просто суше или длиннее», это стоит учитывать.
**Answer:** _(Researcher)_
