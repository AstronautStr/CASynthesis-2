# Decision Tree — граф решений проекта (v3)

_Ведёт Researcher. Карта РАЗВИЛОК для навигации (где мы, что выбрано, что впереди),
НЕ летопись — полное «почему» и детали в `decisions.md`. Перенос в obsidian
(`ResearchMap.canvas`) — отдельным запросом._

## Правила ведения (чтобы граф не зарастал)
- **Вершины = вопросы И ответы.** Обычно чередуются (Q → ответ(ы) → новые Q → …), но
  НЕ обязательно явно: нода-ответ может неявно нести вопрос («какие бывают?»), и под ней
  висят под-ответы-варианты (напр. «вручную фичи» → конкретные фичи). Корень особый.
- **Эксперимент** (прослушивание реализации) — тоже вершина; может иметь НЕСКОЛЬКО
  родителей (опции, которые в него вошли) и порождать несколько вопросов. Итог —
  одной строкой; полный фидбек/диагноз в `decisions.md`. → граф это DAG, не дерево.
- **Предпосылка без альтернатив живёт ВНУТРИ родительской ноды** (напр. «низшая
  мода = f0» в Laplacian; «синтезатор» в корне). Оспорим → вытаскиваем её в
  отдельную развилку и ветвимся параллельно.
- **Инженерные задачи НЕ на графе** (огибающая/ADSR, сглаживание биений, кросс-фейд,
  sounddevice, slot-pool …) — это `decisions.md`/`log/`, не развилки рисёрча.
- **Непройденные ветки не удаляем** — висят со статусом, чтобы можно было вернуться.

## Легенда
🟩 выбрано/принято · 🟨 активная ветка · 🟥 отклонено · ⬜ законсервировано/отложено ·
🟦 кандидат/идея/не построено
формы: ◇ ромб = вопрос · ▭ прямоугольник = ответ · ▱ параллелограмм = эксперимент

```mermaid
graph TD
    ROOT(["🌱 Conway's Life ведёт ТЕМБР синтезатора<br/>(высота — несущая нота) · предпосылка: синтезатор"]):::root

    ROOT --> Q1{"Единица озвучивания?"}:::q
    Q1 --> A_field["поле целиком"]:::rejected
    Q1 --> A_cell["клетка"]:::rejected
    Q1 --> A_obj["ОБЪЕКТ ✓"]:::done

    A_obj --> Q2{"Как получить тембр из формы объекта?"}:::q
    A_obj --> A_seg["сегментация: сейчас связность (8-связная);<br/>common-fate (по движению) — непройден"]:::idea
    A_obj --> Q_dyn{"Динамика объекта во времени → звук?"}:::q
    Q_dyn --> A_motion["движение → glide / doppler / pan<br/>(frame: co-moving vs fixed) — непройден"]:::idea
    Q_dyn --> A_events["жизнь объекта: рождение/смерть/слияние/коллизии<br/>→ звуковые события — непройден"]:::idea
    Q_dyn --> A_wavetable["wavetable: период цикла → форма волны<br/>(§A, альт. стратегия) — непройден"]:::idea
    Q2 --> A_manual["вручную: интерпретируемые фичи → параметры<br/>— отложен (вернётся слоем поверх)"]:::paused
    A_manual --> A_size["первая попытка: size → одна гармоника k<br/>— отвергнут (Life мелок → писк; синус скучен)"]:::rejected
    A_manual --> A_axes["прочие оси формы (docs §G, непройдены):<br/>jaggedness→distortion · symmetry→consonance · activity→tremolo · order/chaos→noise · elongation→detune"]:::idea
    Q2 --> A_auto["автоматически: форма → вектор преобразованием ✓<br/>(не конструируя фичи вручную)"]:::done

    A_auto --> Q3{"Способ авто-маппинга?"}:::q
    Q3 --> A_embed["обученный эмбеддинг — непройден"]:::idea
    Q3 --> A_tr["фиксированное преобразование ✓<br/>(выбор приёмов обслуживает стенд mapping_bench)"]:::done

    A_tr --> Q4{"Каким приёмом преобразования?"}:::q
    Q4 --> A_fft["FFT — законсервирован"]:::paused
    Q4 --> A_wal["Walsh — законсервирован"]:::paused
    Q4 --> A_rnd["Random — baseline"]:::paused
    Q4 --> A_grn["Granulo — законсервирован"]:::paused
    Q4 --> A_zgp["Zernike / Gabor / PCA — не построены"]:::idea
    Q4 --> A_lap["Laplacian ✓<br/>низшая мода = f0"]:::active

    A_lap --> E1[/"ЭКСП-1 · живой Laplace, большое поле<br/>тёмно · крупные формы однородны<br/>диагноз: слушаем низ спектра (Вейль/Kac)"/]:::exp
    E1 --> Q5{"Яркость / различимость,<br/>не теряя высоту?"}:::q
    E1 --> Q_op{"Оператор графа честен?<br/>(равновесный 8-связный анизотропен — Shape-DNA)"}:::q
    Q_op --> A_lapW["взвешенный оператор (диагональ 1/√2 / котангенс)<br/>— кандидат на слух (ближе к Shape-DNA)"]:::idea
    Q5 --> A_spread["A — spread ✓"]:::done
    Q5 --> A_alpha["D — alpha ✓"]:::done
    Q5 --> A_vec["B — формозависимые амплитуды<br/>(собств. векторы) — реактивирован"]:::idea
    Q5 --> A_norm["C — нормализованный лапласиан<br/>— отклонён (отвязал бы высоту)"]:::rejected

    A_spread --> E2
    A_alpha --> E2[/"ЭКСП-2 · крутилки spread + alpha<br/>разные формы = разный спектр ✓ · музыкально 3–4 разнесённых мод · много мод → шумный верх<br/>диагноз: НЕ алиасинг (гвард есть) → roughness плотного негарм. верха (Вейль + Plomp-Levelt) + спад 1/i^α по номеру, не по частоте"/]:::exp

    E2 --> Q6{"Музыкальность за<br/>пределы 3–4 мод?"}:::q
    Q6 --> A_prof["профиль: мало разнесённых мод<br/>= музыкально — принято (природа приёма)"]:::done
    Q6 --> A_tilt["частотный спад 1/(f/f0)^α<br/>— кандидат (отдельной опцией)"]:::idea

    E2 --> Q7{"Крупные формы звучат<br/>одинаково — почему?"}:::q
    Q7 --> A_flood["убрать окно 8×8 — считать весь объект одним графом<br/>(причина: окно режет крупную форму до центра)<br/>— кандидат (риск: гуще верх; ломает контракт с гарм.)"]:::idea

    classDef root fill:#ffd8a8,stroke:#d9480f,stroke-width:2px,color:#000
    classDef q fill:#ffffff,stroke:#495057,color:#000
    classDef done fill:#d3f9d8,stroke:#2b8a3e,color:#000
    classDef active fill:#fff3bf,stroke:#e67700,stroke-width:2px,color:#000
    classDef rejected fill:#ffe3e3,stroke:#c92a2a,color:#000,stroke-dasharray: 4 3
    classDef paused fill:#e9ecef,stroke:#868e96,color:#000
    classDef idea fill:#d0ebff,stroke:#1971c2,color:#000
    classDef exp fill:#e5dbff,stroke:#7048e8,stroke-width:2px,color:#000
```
