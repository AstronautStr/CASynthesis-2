# Current Implementation State

_Снапшот живого состояния. Полная история реализации — `memory/archive/current-history-2026-06-16.md` и `memory/log/`; решённые вопросы — `memory/archive/questions-resolved.md`._

## Implemented (снапшот — детали в log/ и archive/)

**Прототипы и ядро**
- **Модуляризация (2026-06-23, бит-в-бит):** монолит `gol_synth.py` (был 1876 строк)
  разнесён на плоские `casynth_*.py` (иерархия `config ← engine ← session/ui ← gol_synth`,
  без циклов; детали — log/2026-06-23-modularize-gol-synth.md):
  - `casynth_config.py` — ВСЕ константы (геометрия/аудио/слоты/ADSR/палитра/`_RAMP`);
    pygame-free, `__all__` экспортит и `_`-имена. _(Конвенция «константы вверху файла
    прототипа» теперь = вверху этого файла; формулировку в CLAUDE.md обновить отдельно.)_
  - `casynth_engine.py` — `step/hsv/midi_to_freq/note_name/analyse/render_chunk_laplacian/SlotPool`.
  - `casynth_session.py` — `_dump_session/replay_session/_replay_cli` (read-side рекордера;
    write-side `replay_controls` остался в `main()`, схема не менялась).
  - `casynth_midi.py` — `MidiInput` (+опц. `mido`).
  - `casynth_ui.py` — `_make_piano/pattern_preview_surf` + `draw_frame(screen,fonts,state,lay,rt)`
    (ВЕСЬ рендеринг). `main()` (801 стр.) остался владельцем layout-construction/event-loop/
    аудио-MIDI-потоков; `lay`=SimpleNamespace тех же rect-объектов, что мутирует loop.
  - **Гейты:** golden-master аудио (`artifacts/_golden_master.py` → `_ref.npz`, байт-в-байт)
    + dump-кадра UI (`CASYNTH_DUMPFRAME=path`, бит-в-бит vs `_ui_before.png`). Предок
    `gol_life_synth_laplacian.py` НЕ тронут (его копии движка намеренно разошлись).
- **`gol_synth.py`** — мульти-движковый прототип (наследник `gol_life_synth_laplacian.py`):
  тот же живой движок/UI, но приём маппинга выбирается на лету **рядом вкладок** в тулбаре
  из общего реестра `casynth_core.ENGINES` (5 приёмов со стенда: FFT/Walsh/Random/Laplace/
  Granulo). Панель крутилок **динамическая** — показывает атрибуты выбранного движка
  (Laplace: part/spread/alpha/shape/harm/full; гармонические: только part). Значения
  атрибутов **помнятся по каждому движку** (`state['engine_params']`) → возврат к движку =
  тот же звук. Маркер `fullshape` (full=1 по дефолту реестра) ветвит вызывающий код
  (`gol_synth.py:374`): `sub` целиком в Лапласиан vs `extract(sub,8)` для гармонических.
  **ADSR-огибающая** (synth-wide, отдельный UI-блок «ENV» под движковыми ручками):
  Attack/Decay/Sustain/Release привязаны к ЖИЗНИ моды (нет per-note on/off — «нота» =
  рождение/смена/смерть моды на топ-событии). Огибающая = нормированный 0..1 множитель на
  амплитуду моды из формы, тикает раз на чанк в `SlotPool` (`_env_phase`/`_env_level`/
  `_advance_env`). A: 0→пик на рождении; D: пик→sustain-уровень; S(0..1): держится всю жизнь
  моды; R: спад→0 на смерти/смене (прежняя release-механика tail-пула без изменений). Дефолты
  (A→1 чанк, D=0, **S=1**, R=50ms) = `level≡1` → `amp_tgt=amp_new` бит-в-бит как раньше.
  Щипок = S=0; пэд = S=1. `render_chunk` не тронут.
  **Лог сессии (read+write):** `CASYNTH_RECORD=1` пишет пер-кадровый `replay_controls` —
  снапшот ВСЕХ контролов (engine + note/vol/A/D/S/R + полный `engine_params`, авто-захват
  любой ручки). Чтение: `python gol_synth.py replay <ts>` (`replay_session`) — офлайн-рендер
  через те же analyse/SlotPool/render + sample-fidelity vs записанного WAV. Конвейер «запись→
  реплей→валидация фикса» замкнут. Правило: новый контрол = сразу весь путь read+write лога.
  Аудио-движок (SlotPool/глайд амплитуд): у гармонических движков частоты слотов постоянны →
  кросс-фейд хвостов не активируется; у Laplace — хвосты как прежде. Старт = laplacian с
  дефолтами реестра (part=12/spread=0/alpha=1/shape=0/harm=0, **fullshape=ON**): на формах
  ≤8×8 спектр = как у 8×8-лапласиан-прототипа, на крупных — граф по всей форме (отличается).
- `casynth_core.py` теперь содержит реестр `ENGINES` / `ENGINE_BY_ID` (id, label, map-fn,
  param-specs) — чистые данные, без pygame; единый источник для селектора движков.
- **`gol_life_synth_laplacian.py`** — предшественник (одно-движковый Laplace), оставлен как есть: каждый связный объект → голос,
  спектр = граф-Лапласиан формы (√λ → частоты мод), негармонический «металлический» тон.
  sounddevice callback-стрим (gapless, развязан с 60 fps). Живые крутилки тембра
  **partials/spread/alpha/release** (экранные слайдеры, меняются на лету; partials =
  число партиалов на объект, 1–20, дефолт 12 — твик из laplacian_explainer.html,
  пишется в лог сессии). `MAX_MODES_PER_OBJ`=20 — потолок/ёмкость слотов. Аудио-движок:
  active-слот + пул tail-слотов с бесшовным переносом хвоста (overlap-кросс-фейд без
  щелчка, хвосты наслаиваются в пэд). Громкость пре-клип + level/clip-метр.
- `gol_life_synth.py` — **legacy v1**: size→harmonic, pygame.mixer.
- `casynth_core.py` — общая либа алгоритмов маппинга форма→(freqs,amps): пять `map_*`
  (fft2d/walsh/random/laplacian/granulo), numpy/scipy без pygame.
  `map_laplacian(patch, f0, n, spread=0, alpha=1, shape=0, harm=0, fullshape=False)`;
  низшая взятая мода всегда = f0; дефолты воспроизводят исторический выход бит-в-бит.
  - **shape** [0,1]: blend между rolloff `1/i^α` (shape=0, дефолт) и проекцией краевого
    возбуждения `e_i=deg_i` на собств. векторы (shape=1 → амплитуды зависят от формы).
    Частоты не затрагиваются. Принято Researcher 2026-06-20; принято на слух 2026-06-22.
  - **harm** [0,1]: гармоническое квантование — притягивает частотные отношения мод
    `r_k=freq_k/f0` к ближайшему целому (`(1−harm)·r + harm·round(r)`), ось металл→хор→
    гармоника. Трогает ТОЛЬКО частоты; форма выбирает занятые гармоники (round-к-ближайшей,
    не 1..n). Принято Researcher (code-review) + на слух 2026-06-22.
  - **fullshape** (bool): при True вход `patch` = полный bbox объекта (не 8×8 окно) → граф
    строится по ВСЕМ клеткам; потолок `MAX_LAPLACIAN_NODES=256` с решёточной децимацией
    (`patch[::step,::step]`). Дефолт ядра False (бит-в-бит); реестровый Laplacian = True.
    Реализован 2026-06-22 (дизайн принят Researcher); прослушивание «центр vs вся форма» —
    в очереди.
- `mapping_bench.py` — discover-стенд (матрица осцилляторы × приёмы).
- `patterns.py` — 29 паттернов в 9 категориях (верифицированы).
- `tests/test_casynth_core.py` — **30 тестов** инвариантов (без pytest-зависимости):
  базовые + критерии shape (B/C/D) + harm (бит-в-бит при harm=0, целые отношения при
  harm=1, f0-привязка, неприкосновенность амплитуд) + fullshape (бит-в-бит дефолт,
  ≤8×8 = тот же спектр, периферия меняет спектр, инвариантность на произвольной маске,
  потолок узлов).
- **`gol_vocoder.py`** — offline-прототип креативного ресинтеза («GoL-вокодер»):
  simulated annealing подбирает бинарное поле, чей спектр `casynth_core.map_laplacian`
  приближает log-mel-огибающую целевого WAV; эффект «цель сквозь GoL-металл», не копия
  (инверсия невозможна — коспектральность, Kac 1966). CLI: `--selftest`, `target.wav`,
  есть и `--dynamic` (меняющаяся во времени цель, T4). Переиспользует настоящий движок →
  паритет звука. **Реализован, ревью/прослушивание Researcher НЕ пройдено** (REQ T3 в
  questions.md ещё `[OPEN]`, Answer не заполнен).
- **`laplacian_explainer/`** — discover-инструмент/обучалка (JS дублирует map_laplacian,
  известный trade-off). **Расфасован из монолита в папку:** `index.html` + `styles.css` +
  `src/{params,linalg,laplacian,render,excitation,audio,app}.js` (классические `<script>`,
  общая глобаль, порядок зависимостей). REQ-1 (mode-toggle `modeMask` + слайдер `shape` +
  удаление matrix-блока) — уже был в коде. **REQ-2 реализован 2026-06-23** (`src/excitation.js`):
  обучающая «Часть 1.5» — секция краевого возбуждения с 4 панелями A/B/C/D + пресеты
  (Блок 3×4 / Змейка 12 / Струна 1×8 / Blinker / Blinker+1). A: форма в тепле по `e=deg`,
  тумблер deg↔дефицит; B: фигура Хладни `φ_k` + solo; C: поклеточно `e·φ` + столбик
  rolloff→проекция по `shape`; D: симметрия глушит моду (Σ⟨e,φ⟩=0 на blinker, ≠0 на
  blinker+1 — проверено численно). Секция самодостаточна (своё состояние, свой solo-audio,
  фикс `EX_F0=220`), песочницу/`computeLaplacian` не трогает; проекция бит-в-бит формуле
  ядра (`e=diag(L)`, `|⟨e,φ_k⟩|`, нормировка max=1). **REQ-1 и REQ-2 explainer ПРИНЯТЫ
  Researcher 2026-06-23** (`[ANSWERED]` в questions.md): паритет с `casynth_core.map_laplacian`
  подтверждён численно (`verify_req2_parity.py`: 5 пресетов × shape∈{0,0.5,1} → dAmp=0,
  dFreq~1e-13; критерий D — blinker Σ=0, blinker+1 Σ=−0.816 против настоящего ядра). Открыт
  только слуховой пласт (читаемость раскрасок, solo-моды на слух — за Пользователем, не блокер).

**Перцептивные оси (заведены)**
- size (area) → harmonic number k (v1) / набор Laplace-мод (v2)
- density (bbox fill) → amplitude (1/k^0.7 rolloff)
- centroid X → stereo pan
- Voice cap `MAX_VOICES = 24`; несущая — экранное пианино C3–C5 (latched)

**UI:** рисование/стирание клеток, play/pause/step/random/clear/скорость, сайдбар
паттернов с drag-and-drop (ghost-превью), легенда гармоник, level/clip-метр.
- **Спектр-полоска (2026-06-24):** read-only визуализация в пустой зоне тулбара под
  колонкой крутилок движка (`lay.spec_rect`, строится в `main()`; рисуется
  `casynth_ui._draw_spectrum`). Столбики мод выбранного движка: вертикальная линия на
  каждой частоте `voices[].freqs` высотой = `amps` (СЫРОЙ выход движка, ДО ADSR/хвостов/
  клипа — спектральный отпечаток формы вкл. shape/harm/spread). Лог-ось X (`SPEC_F_MIN..
  SPEC_F_MAX`=20..20000 Гц в config), метки декад 100/1k/10k, тонкий маркер несущей f0.
  Бары раскрашены цветом объекта поля (`color[label_idx]`) → спектр привязан к форме.
  Источник данных — `rt.voices`/`rt.color` (уже в draw-потоке): ни FFT, ни потоков, ни
  захвата аудио. Не контрол → session-лог не затрагивает. Аудио golden-master PASS
  (звук не тронут); UI-эталон `_ui_before.png` легитимно расходится (доп. рисунок в
  ранее пустой зоне) — обновить при следующем касании UI-гейта.

**Статус ревью:** Researcher ревьюит ТОЛЬКО библиотеку алгоритмов `casynth_core.py`
(карты форма→(freqs,amps) + данные реестра `ENGINES`), НЕ прототип-приложения. Алгоритмы
`map_*` и крутилки spread/alpha/release **приняты** 2026-06-17 (DEV-2 закрыт, DEV-1 —
ручной headroom). Ручка **shape** (Шаг B) **принята** 2026-06-20 (критерии A–E) и на слух
2026-06-22. Ручка **harm** **принята** 2026-06-22 (code-review критерии A–G, 30/30 тестов;
на слух — лечит регистровую «грязь»). Реестр `ENGINES` — чистые данные поверх принятых
функций. Прототип `gol_synth.py` (UI/селектор/SlotPool) — инженерия, под дизайн-ревью не
попадает.
- **Реализовано, ревью НЕ закрыто:** `fullshape` (дизайн принят 2026-06-22, реализация
  есть, формального code-review реализации + прослушивания нет); `gol_vocoder.py` (REQ T3
  `[OPEN]`). Это вынесено в отчёт dream — нужны решения Researcher, не библиотекарская правка.
- **Реализовано и ПРИНЯТО Researcher (код):** правки `laplacian_explainer/` — REQ-1
  (mode-toggle/shape/удаление matrix) + REQ-2 (секция краевого возбуждения A/B/C/D),
  `[ANSWERED]` 2026-06-23; остался только слуховой/визуальный пласт за Пользователем.

## Known issues
- Segmentation не wrap-aware: объект на шве тора временно распадается на два голоса.
- Нет трекинга идентичности объектов между поколениями (нет birth/death-событий, коллизий).
- Заведено ~3 из ~8 спроектированных перцептивных осей — диапазон тембра узок.
- Клиппинг при плотном поле + яркая настройка — ручной headroom (метр+громкость, DEV-1).
  Researcher (2026-06-17): для фазы прослушивания этого достаточно; в музыкальной рабочей
  зоне (≈3–4 моды) энергия мала. Авто-нормировку уровня вернуть, только если плотные яркие
  сцены станут рабочим режимом.

_Сняты разработкой (для истории — детали в decisions.md):_
- «Грязь» на нижних нотах (roughness некратных партиалов в критполосах) — **решено `harm`**
  (притяжение к гармонической решётке; принято на слух 2026-06-22).
- «Крупные формы звучат похоже» — адресовано `shape` (формозависимые амплитуды разделяют
  формы их центром, принято на слух) + `fullshape` (вся форма в графе, прослушивание в очереди).

## In-flight
Крутилки spread/alpha/release, **shape** и **harm** — прослушаны и приняты (2026-06-22).
Развилка **extract 8×8 закрыта**: реализован флаг `fullshape` (граф по всей форме, дефолт
реестрового Laplacian = ON) — формальный извод из decisions.md 2026-06-17/06-22.

**Ожидает прослушивания / закрытия ревью:**
- `fullshape` на живом поле — A/B «центр+shape vs вся форма»: даёт ли вся граница
  различимость СВЕРХ уже достигнутой shape под 8×8 (на «различимы ли крупные формы вообще»
  ответ уже да). Плюс формальный code-review реализации ядра.
- `gol_vocoder.py` (T3) — прослушать эффект «цель сквозь GoL-металл» + закрыть REQ.
- Правки `laplacian_explainer/` (REQ-1 mode-toggle + REQ-2 секция краевого возбуждения
  A/B/C/D) — **код принят Researcher 2026-06-23** (паритет численно подтверждён); осталось
  слуховое/визуальное подтверждение Пользователя (solo-моды, читаемость раскрасок) — не блокер.

Следующие исследовательские шаги:
- Профилирование законсервированных FFT/Walsh/Random/Granulo на крупных/плотных полях —
  есть живой стенд (селектор движков в `gol_synth.py`).
- **РАЗВЕДКА (ничего не выбрано), decisions.md 2026-06-22:** карта «форма→звук» для ветки
  ОБЪЕКТ — двери-кандидаты A (граничный Фурье → гармоники из контура) / B (форма-как-сеть-
  задержек → пространство/ритм); ось топологии (персистентная гомология). Возврат = развернуть
  A/B в дизайн.

## Mapping bench — discover завершён (2026-06-15)
- `mapping_bench.py` — ревизия завершена, все P0/P1 прошли researcher-ревью.
  Матрица 4×5: осцилляторы Blinker, Toad, Beacon, Clock (Clock_C период-2) × маппинги
  2D-FFT, Walsh-2D (sequency), Random, Laplacian (негармонический), Granulo.
- **Итог batch-прослушивания (Пользователь):** FFT/Walsh/Random/Granulo на мелком
  зоопарке — различие есть, но **невыразительное**; Granulo вырожден. **Laplace выделился**
  — органичный металлический тон, самостоятельная фишка → выбран для второго прототипа.
- Стенд остаётся discover-инструментом; выбракованные приёмы НЕ выброшены, а
  законсервированы (профили — в `decisions.md`).

## Open research — профилирование маппингов (параллельная ветка, не блокирует прототип)
Стратегия: **не выбрасывать тупиковые ветки**, набрать зоопарк приёмов и собрать
**профиль/характер каждого** на разных режимах автомата → карта «какой приём для чего
годится». Рамка и текущие профили 5 приёмов — в `memory/decisions.md`.
- Ближайшая измерительная задача: прогнать законсервированные приёмы (FFT/Walsh/Granulo)
  на **крупных/плотных полях** — проверить гипотезу «шумный на малом → структурный на
  большом». Формат стенда под крупные режимы не заточен (отдельная задача).
- Разные приёмы могут управлять РАЗНЫМИ параметрами синта (тембр/огибающая/модуляция/
  пространство) — карта это и должна развести.

## Candidate next steps
See `memory/decisions.md` (индекс вверху) for prioritisation by Researcher.
- Remaining perceptual axes: jaggedness → distortion/inharmonicity, activity → tremolo,
  symmetry → consonance, order/chaos → noise blend, elongation → detune/vibrato
- Wrap-aware segmentation (union-find across toroidal seam)
- Object identity tracking (voice allocation with birth / death / merge / split events)
