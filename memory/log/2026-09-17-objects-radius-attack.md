# 2026-09-17 — Objects: ручной диапазон Radius x, Attack возбуждения, каталог R1/R2/A1 + пресет

ТЗ: `memory/req-objects-radius-attack-2026-09-17.md` (Researcher), фидбек
`memory/research/objects-feedback-radius-attack-2026-09-17.md`, preflight
`memory/research/objects-radius-attack-preflight-2026-09-17.json`. Роль: Developer (основная сессия).
Статус: **реализовано, каталог собран и запинен, измерения сняты, `check.py` зелёный; ждёт технической
сверки Researcher, затем пользователь играет и дописывает Notes.**

## Коммиты

1. `9ed1ddc` **Radius x без потолка + Min/Max диапазон слайдера на сторону + полные маски.**
   Реестр: `radius_mul` валидируется как любое конечное значение ≥ 0 (spec `0..inf`); `EngineSpec.ranges`
   = `{param: (min, max)}` — начальный поддиапазон слайдера (Objects: `RADIUS_RANGE = (0.25, 4.0)`),
   `registry.validate_range / default_range`; сам реестр при правке границ не меняется. Раннер: команда
   `set_range side/name/lo/hi` (журналируется; проверка против движка, который сторона БУДЕТ иметь; spec lo ≤
   min < max ≤ spec hi), диапазоны хранятся per (сторона, движок) как память параметров, в
   `export_state/from_state` (`ranges`, отсутствие = дефолты) и в `snapshot()['ranges']`; значение раннер не
   прижимает. Сцена: необязательный `param_ranges {side: {engine: {param: [min, max]}}}`; рекордер/каталог несут
   его в условиях записи (save → `bench_scene` → Continue). `figures.full_cover_radius/covers_field`: радиус от
   `hypot(rows/2, cols/2)` — эквивалентная полная маска без возведения радиуса в квадрат (1e300, inf — тоже
   полная); `display()` фигуры: `covers_all` (только Disk; одиночная клетка R=0). Стенд: строка «range» под
   слайдером с полями Min/Max (общая модель `TextEdit`; Enter/уход из поля — коммит, Esc — отмена, Tab —
   другая граница, плохое число отклоняется на месте с причиной `not a number / min < 0 / min must be < max`),
   слайдер линейный по диапазону выбранной стороны с шагом 0.001 (значение показано с 3 знаками), диапазон,
   исключающий значение, прижимает его один раз обычным `set_param` (два коммита в одном блоке не затирают друг
   друга: `_range_pending/_clamp_pending`), хоткеи неактивны при вводе, Esc не закрывает окно при фокусе в
   поле; круг, покрывающий всё поле, рисуется рамкой в цвете фигуры внутри края поля (вложенно по фигурам) +
   подпись «N circles cover the whole field». В коммит вошли три файла Researcher (ТЗ, фидбек, preflight):
   гейт читает preflight.
2. `0fa58a1` **tests: S/N-тест Notes следует окну заметок** — `test_notes_button_editor_extraction`
   проверял старый режим `'notes'` внутри окна стенда, который окно заметок 2026-09-17 заменило; гейт S/N был
   красный на master (проверено на чистом HEAD в worktree). Не связано с ТЗ, починено отдельно.
3. `8a8fbfb` **Objects v3: Attack.** Ручка `attack_ms` («Attack», 0…20 ms, default 0). На семпл:
   `u = q·u + (1−q)·p`, банк ведёт `u`; `q = exp(−ln 9/(SR·attack_ms/1000))`, 0 при Attack 0; состояние `zu`
   на слот, обнуляется вместе с импульсными состояниями при уходе банка в хвост (хвост возбуждения не получает);
   ручная смена — линейная рампа `q` 20 мс (`qq`, четвёртый счётчик `ints`). При `q = 0` формула даёт `u = p`
   точно → Attack 0 и конец рампы к нулю — побитово прежний путь v2 (скалярный эталон N4 без атаки проходит
   против ядра v3 с расхождением 0.0). Компенсации громкости нет. Моменты пакетов, `a=e/(e+2)`, настройка и веса
   не тронуты. `model_version=ca_object_resonators_n4_v3`, `STATE_VERSION 3` (+`zu`, `qq`, `ints[4]`); снимок v2
   модели v2 восстанавливается как Attack 0 с нулевым состоянием атаки; набор параметров без ключа = 0.
   `display()`: `attack_ms, q, q_target, attack_ramp_left`. Стенд: `ROW_H 26→24` (13 ручек + строка range +
   строки фигур — окно 974 px). Сцены прежних каталогов (`n4_*`, `ol_*`) и их сборщики несут `attack_ms: 0`
   (звук тот же; их записи остаются запинены на свои коммиты по правилу 2026-09-17: старые записи в текущем
   коде сцена отвергает как «missing engine_params», открываются «Continue in version»).
4. **Каталог, отчёт, память** (этот коммит): `demos/build_objects_radius_attack.py` → `demos/ora_{r1,r2,a1,
   user034}.json` → `lab_catalog/objects_radius_attack_2026_09_17/` (4 записи, запинены на `8a8fbfb`,
   гипотезы ТЗ в Notes), `run_objects_radius_attack.bat`, `demos/objects_radius_attack_report.py` →
   `demos/results/objects_radius_attack/report.{json,md}`; подписи стенда (строка над полем и строка фигур)
   укорачиваются, чтобы влезать в ширину.

## Тесты — `tests/test_objects_radius_attack.py` (19, гейт 1n)

Радиус: реестр/сцена без потолка и с отказами (отрицательные, inf, nan, bool, строки; подсказка `ranges`
проверяется при `register`); `disk_mask` ×1/×2/×8/×32 = независимый поклеточный расчёт торового расстояния,
полное покрытие от границы, R=0 — одна клетка; движок ×32: `covers_all`, `e` каждой фигуры = все изменения поля;
Own игнорирует радиус. **Радиусные пары против независимых масок** (71 переход): каждый пакет каждого банка =
рождения в текущем круге + смерти в предыдущем (0 расхождений), чужая часть по банкам, распределение размеров
пакетов, итоги preflight (R1 ×1 [0,0]/[0,71], ×2 [284,0]/[71,71]; R2 ×1 [0,0]/[118,71], ×8 [392,519]/[141,71],
×32 [1312,519]/[141,71]), совместная эволюция = объединение двух независимых; blinker, сдвинутый на 8 столбцов
(13.3…14.1 клетки от центра приёмника), при ×2 (R_eff 10.66) даёт 0 чужих, при ×3 (16.0) снова 4 на поколение;
ручка/диапазон никогда не дают пакета (движок и раннер: блоки побайтно равны контролю). Раннер/сцена/запись:
валидация `set_range`, независимость А/Б, память через смену движка, `factory` не трогает, snapshot без
`ranges` = дефолты, `param_ranges` сцены, запись → `bench_scene` → Continue. Стенд headless: поля, коммит
Enter/уходом, Esc, Tab, отказы, прижатие ровно один раз (журнал `set_param` [32.0, 0.6]), шаг 0.001, рамка
полного покрытия (пиксель цвета фигуры в углу поля), хоткеи неактивны при вводе.
Attack: формула `q` и время 10→90 % (±1 семпл) vs preflight; Attack 0 == скалярный эталон N4 **ровно**; ядро vs
независимый скалярный эталон сглаживания через удары/рампы/переходы через ноль ≤ 1e−12 и точный bypass после
рампы к нулю (двойник с обнулённым `zu` даёт те же блоки); тишина без событий; одинаковые моменты пакетов и
импульсные состояния при 0/4 ms; Continue посреди импульса/рампы/при наложении ударов; новый фронт без сброса
банка; хвост без возбуждения; снимок v2 = Attack 0; уровень/яркость пробы.

## Измерения (`demos/results/objects_radius_attack/report.md`, коммит `8a8fbfb`)

- **Радиус:** таблицы выше совпали с preflight полностью; при ×32 у обоих семейств `covers_all`, у Jam пакеты
  растут с 0…5 до 3…41 клеток. Сдвиг блинкера вне/внутрь круга меняет вклад 0 ↔ 4 на поколение.
- **Attack (проба 110/440/880 Hz, Decay 1.39, на выходе движка):** 1 / 4 / 10 / 20 ms → ВЧ > 3 kHz импульса
  −23.1 / −35.1 / −43.1 / −49.1 dB, RMS отклика −0.8 / −4.8 / −11.1 / −16.8 dB, соседний шаг при равном RMS
  −11.1 / −15.2 / −16.3 / −16.7 dB (preflight: −23.08/−35.09/−43.04/−49.06; −0.81/−4.77/−11.03/−16.74;
  −10.94/−15.38/−16.58/−16.72). Первый семпл сглаженного импульса при 4 ms 0.0056 вместо 0.45.
  Поле A1 при единичных гейнах: моменты пакетов идентичны, Б−А RMS −5.82 dB, энергия > 3 kHz −28.7 dB,
  соседний шаг при равном RMS −14.75 dB.
- **Уровни (side_gain Б):** R1 0.65 (А−Б было −3.80 dB → −0.06), R2 0.69 (−3.26 → −0.04), A1 2.05 (+6.25 →
  +0.01), U0 1.0 (0.00). Пики после компенсации ≤ 0.263, клипа нет. Continue с 6 с точен (200 блоков, включая
  диапазоны). p99 обеих сторон после прогрева: 2.16 / 3.61 / 4.55 / 4.68 ms (< 7.98).
- **Живые сцены на реальном устройстве** (LiveEngine, 12.5 с каждая, переключение на Б на 6 с): underrun 0,
  clip 0/0 на всех четырёх.
- Каталог: 4 записи, replay `match`, pinned `8a8fbfb`, Notes с гипотезами.

## Решения Developer (не в ТЗ, см. `questions.md`)

- Поле пресета U0 (ТЗ поле не задаёт): поле R2/A1 (Jam p3 + Octagon II p5 — полиритм, который пользователь
  отметил), диапазон 0.25…1 (шаг 0.01 = 6 px).
- `copy_side` / `factory` / `copy_spectrum` диапазоны не переносят и не сбрасывают (диапазон — состояние UI
  стороны); значение вне диапазона после копии сторон стоит у края слайдера, звук не меняется.
- Снимок v2 читается движком (Attack 0), но сцена/раннер остаются строгими к старым записям (правило
  2026-09-17) — записи прежних каталогов открываются в своей версии.
- Тест S4 `test_save_replay_fixed_experiment_in_fresh_process` один раз упал на гонке `gen` живого раннера с
  концом записи (сохранение шло, пока движок крутился); отдельно проходит. Не правил.
