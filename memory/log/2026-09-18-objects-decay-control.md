# 2026-09-18 — Objects: контроль затухания D4 (подобранный Fixed / Modal, тот же Fixed / длинный Fixed)

ТЗ: `memory/req-objects-decay-control-2026-09-18.md` (Researcher), preflight
`memory/research/objects-decay-control-preflight-2026-09-18.json` (на `255af20`). Роль: Developer (основная сессия).
Статус: **каталог собран и запинен (`4f7e072`), измерения сняты, все проверки отчёта прошли; ждёт сверки Researcher,
затем пользователь слушает и дописывает Notes.** Слухового результата нет.

Коммиты: `60b76b6` (пакет Researcher: ТЗ D4, preflight, ревью D1–D3, audit evidence; Notes пользователя D1–D3),
`4f7e072` (сборщик, сцены, bat, README, тесты), затем каталог + отчёт + память.

## Что сделано

1. **Код движка не менялся** — только сборка новых сравнений на Objects v6. Звуковой набор тот же, что у D1–D3
   (`1fdf8d1`), поэтому D3 и новые записи открываются в текущей версии без «Continue in version».
2. **Сборщик** `demos/build_objects_decay_control.py`: сцены выводятся из **встроенной сцены записи D3**
   (`lab_catalog/objects_decay_2026_09_18/20260918-204155-4ff923`, `demo_scene`), а не из текущих default/UI. Поле —
   массив `cells` preflight (те же 29 клеток, что в D3). Условие меняет только Decay law, Decay и постоянный side gain
   стороны D3, от которой отсчитывается: подобранный Fixed = А D3 с Decay 0.671529 s, ×1.220467; прежний Modal = Б D3,
   ×1.21; прежний длинный Fixed = А D3, ×1.0. `check_against_preflight()` сверяет константы ТЗ, часы, поле, общие
   параметры и пары с preflight при каждом вызове. `--calibrate` измеряет RMS при единичных гейнах: подобранный Fixed
   −35.6573 dBFS → множитель к длинному Fixed **1.220467** (совпал с ТЗ до 6-го знака), Modal −35.5628 → 1.207262
   (сохранён прежний 1.21 по ТЗ).
3. **Сцены** `demos/od_d4_control.json` (D4.1: А подобранный Fixed / Б прежний Modal), `demos/od_d4_anchor.json`
   (D4.2: А тот же Fixed / Б прежний длинный Fixed); `run_objects_decay_control.bat` открывает экран каталога.
4. **Каталог** `lab_catalog/objects_decay_control_2026_09_18/`: D4.1 `20260918-221025-d81b66`, D4.2
   `20260918-221023-9e3dba` (порядок на экране D4.1, D4.2), 18 с = 794112 отсчётов, 107 поколений, без сценария и
   вмешательств; в описании записи условия названы явно; Notes = гипотезы ТЗ дословно (сверено программно с цитатами ТЗ).
   Исходный каталог D1–D3 и его Notes не тронуты.
5. **Отчёт** `demos/objects_decay_control_report.py` → `demos/results/objects_decay_control/report.{json,md}`.
6. **Тесты** (гейт 1q, теперь 25): `D4ControlTests` — сцены отличаются от D3 только как в ТЗ, на диске == сборщик;
   D4.1, D4.2 и D3 в одном цикле блок за блоком: А == А, Б D4.1 == Б D3, Б D4.2 == А D3; среднее применённой gamma
   Modal в замкнутой форме (геометрическая сумма сглаживания по блоку) == preflight до 1e−9, контроль ≤ 1e−4.
   `OBJECT_SCENES` дополнен сценами D4.

## Измерения (`demos/results/objects_decay_control/report.md`, коммит `4f7e072`)

- **Среднее (независимо):** посэмпловая рекурсия `g ← k·g + (1−k)·gt` в numpy от состояний начала блока и целей блока
  (k = exp(−1/(44100·0.020)) посчитан отдельно, == движку), J(s) = моды j < ndrive слотов ACTIVE, каждая пара
  «мода–сэмпл» с равным весом, окно [132300, 793800), частичные блоки по отсчётам. **5 293 928 пар, gamma 10.286612669487
  1/s, T 0.6715286656 s** (== preflight, −5.4e−14); концы блоков рекурсии == `gam_a` движка точно (0.0 на 2256 блоках);
  все активные банки Modal на своей gamma. Контроль ln1000/0.671529 = 10.286607546334 1/s → **−0.000050 %** (предел
  0.01 %); применённая движком gamma Fixed (−SR ln r) та же. Ниже/выше контроля 3 650 429 / 1 643 499 пар, min/max
  4.9696 / 86.3469 1/s — как в preflight. Средняя целевая gamma 9.81862346094 — только диагностика.
- **Идентичность:** хеши сырого int16 interleaved всех трёх условий == preflight; А D4.1 == А D4.2 (байты WAV и хеши),
  Б D4.1 == Б D3 == Modal preflight, Б D4.2 == А D3 == длинный Fixed preflight; длины 794112 у A/B/monitor; журналы —
  только start + 107 шагов на тех же отсчётах, что в D3. По блокам (D4.1, D4.2, D3 в одном цикле): поле и поколение,
  фигуры (id, слоты, клетки, центры), у каждого активного банка частоты, веса и их рампы, пакеты/импульс, Attack, панорама
  равны у трёх условий; два подобранных Fixed равны во всех массивах движка; Fixed-стороны точно на r(0.671529) / r(1.39)
  без своих gamma. Различаются только состояния резонаторов, потери и постоянный side gain.
- **Уровни (RMS обоих каналов за 3…18 с, из записей):** подобранный Fixed −33.9258, Modal −33.9061, длинный Fixed
  −33.9268 dBFS (== preflight), **разброс 0.021 dB** (предел 0.1). Пики int16 / до клипа 0.1899 / 0.1171 / 0.1626; клипа
  нет, всё конечно.
- **Записи:** replay A/B/monitor match у обеих, pinned `4f7e072`; Continue из сохранённого конечного снимка 125 блоков ==
  непрерывный рендер (A/B/monitor, часы равны); исходная запись D3 по-прежнему воспроизводится точно.
- **Время блока** (каждая сцена отдельно): p99 4.6–5.0 ms при бюджете 7.98; единичные максимумы до ~11 ms — шум машины
  (та же сцена D3 дважды подряд: max 4.98 и 11.72 ms), движок тот же, что у D3.

## Не входит в поставку

- **P2 очереди команд** (`runner._params_when_applied`, ветки `copy_side` / `copy_spectrum` берут источник из текущего
  состояния, а не из прогноза очереди) — отдельное открытое исправление (`questions.md`). Новые сцены задают параметры
  сразу и его не задевают. Правка раннера перепривязала бы звуковой набор, поэтому она — отдельная единица работы.

## Гейты

`python check.py` на `4f7e072` + отчёт: **ALL GATES PASS** (в т.ч. golden master, UI-кадр, гейт 1q с двумя новыми
тестами D4); `demos/objects_decay_control_report.py`: все пять групп проверок пройдены.
